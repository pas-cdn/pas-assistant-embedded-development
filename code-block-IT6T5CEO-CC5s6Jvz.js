import { A as jr, B as Hr, D as ve, F as ir, G as Vr, H as ar, I as Wr, J as zr, K as qr, L as Xr, N as Qr, O as Jr, P as Ie } from "./mermaid-VLURNSYL-B08t8RiY.js";
import { r as q, j as v } from "./embed-entry-Ba0tPvER.js";
const oe = [
  {
    id: "abap",
    name: "ABAP",
    import: () => import("./abap-Y8Dl9g_6.js")
  },
  {
    id: "actionscript-3",
    name: "ActionScript",
    import: () => import("./actionscript-3-DQVEcJUW.js")
  },
  {
    id: "ada",
    name: "Ada",
    import: () => import("./ada-vP6ak0IW.js")
  },
  {
    id: "angular-html",
    name: "Angular HTML",
    import: () => import("./angular-html-dA1qUKRV.js").then((e) => e.f)
  },
  {
    id: "angular-ts",
    name: "Angular TypeScript",
    import: () => import("./angular-ts-CXyBC0vY.js")
  },
  {
    id: "apache",
    name: "Apache Conf",
    import: () => import("./apache-BUjz-sD2.js")
  },
  {
    id: "apex",
    name: "Apex",
    import: () => import("./apex-OyTfjCYK.js")
  },
  {
    id: "apl",
    name: "APL",
    import: () => import("./apl-BRmomHzM.js")
  },
  {
    id: "applescript",
    name: "AppleScript",
    import: () => import("./applescript-BPx7YFFu.js")
  },
  {
    id: "ara",
    name: "Ara",
    import: () => import("./ara-Z2fSOxSw.js")
  },
  {
    id: "asciidoc",
    name: "AsciiDoc",
    aliases: ["adoc"],
    import: () => import("./asciidoc-C1T9ziH6.js")
  },
  {
    id: "asm",
    name: "Assembly",
    import: () => import("./asm-BTWLY5ym.js")
  },
  {
    id: "astro",
    name: "Astro",
    import: () => import("./astro--3ZkKqYU.js")
  },
  {
    id: "awk",
    name: "AWK",
    import: () => import("./awk-Fb0P9dkn.js")
  },
  {
    id: "ballerina",
    name: "Ballerina",
    import: () => import("./ballerina-oZK-YekG.js")
  },
  {
    id: "bat",
    name: "Batch File",
    aliases: ["batch"],
    import: () => import("./bat-0FvbqU9S.js")
  },
  {
    id: "beancount",
    name: "Beancount",
    import: () => import("./beancount-DqJEb89h.js")
  },
  {
    id: "berry",
    name: "Berry",
    aliases: ["be"],
    import: () => import("./berry-DODBq_Ff.js")
  },
  {
    id: "bibtex",
    name: "BibTeX",
    import: () => import("./bibtex-EULQRLY5.js")
  },
  {
    id: "bicep",
    name: "Bicep",
    import: () => import("./bicep-BorU73w0.js")
  },
  {
    id: "bird2",
    name: "BIRD2 Configuration",
    aliases: ["bird"],
    import: () => import("./bird2-CSN72va7.js")
  },
  {
    id: "blade",
    name: "Blade",
    import: () => import("./blade-BoRuQEEj.js")
  },
  {
    id: "bsl",
    name: "1C (Enterprise)",
    aliases: ["1c"],
    import: () => import("./bsl-NkNNwerW.js")
  },
  {
    id: "c",
    name: "C",
    import: () => import("./c-eeMepfLm.js")
  },
  {
    id: "c3",
    name: "C3",
    import: () => import("./c3-BY5XW5ym.js")
  },
  {
    id: "cadence",
    name: "Cadence",
    aliases: ["cdc"],
    import: () => import("./cadence-02UX7mW8.js")
  },
  {
    id: "cairo",
    name: "Cairo",
    import: () => import("./cairo-DM6WF2e3.js")
  },
  {
    id: "clarity",
    name: "Clarity",
    import: () => import("./clarity-PKm5CwqM.js")
  },
  {
    id: "clojure",
    name: "Clojure",
    aliases: ["clj"],
    import: () => import("./clojure-CXJfHrL3.js")
  },
  {
    id: "cmake",
    name: "CMake",
    import: () => import("./cmake-BJz8BOTU.js")
  },
  {
    id: "cobol",
    name: "COBOL",
    import: () => import("./cobol-tfDVNHxI.js")
  },
  {
    id: "codeowners",
    name: "CODEOWNERS",
    import: () => import("./codeowners-Bt9yU6NX.js")
  },
  {
    id: "codeql",
    name: "CodeQL",
    aliases: ["ql"],
    import: () => import("./codeql-DHkodjjI.js")
  },
  {
    id: "coffee",
    name: "CoffeeScript",
    aliases: ["coffeescript"],
    import: () => import("./coffee-DGBR_nr_.js")
  },
  {
    id: "common-lisp",
    name: "Common Lisp",
    aliases: ["lisp"],
    import: () => import("./common-lisp-EVqT9Zhp.js")
  },
  {
    id: "coq",
    name: "Coq",
    import: () => import("./coq-B0L9upzn.js")
  },
  {
    id: "cpp",
    name: "C++",
    aliases: ["c++"],
    import: () => import("./cpp-Cj177cuW.js")
  },
  {
    id: "crystal",
    name: "Crystal",
    import: () => import("./crystal-B7r8KTTT.js")
  },
  {
    id: "csharp",
    name: "C#",
    aliases: ["c#", "cs"],
    import: () => import("./csharp-CCgA4A09.js")
  },
  {
    id: "css",
    name: "CSS",
    import: () => import("./css-M7EaDHN_.js")
  },
  {
    id: "csv",
    name: "CSV",
    import: () => import("./csv-CmYOceLb.js")
  },
  {
    id: "cue",
    name: "CUE",
    import: () => import("./cue-ZzumE7IT.js")
  },
  {
    id: "cypher",
    name: "Cypher",
    aliases: ["cql"],
    import: () => import("./cypher-jpdmjtA6.js")
  },
  {
    id: "d",
    name: "D",
    import: () => import("./d-CBagWSwH.js")
  },
  {
    id: "dart",
    name: "Dart",
    import: () => import("./dart-BoQ-8O4a.js")
  },
  {
    id: "dax",
    name: "DAX",
    import: () => import("./dax-CcDT-8dH.js")
  },
  {
    id: "desktop",
    name: "Desktop",
    import: () => import("./desktop-B93p9R9O.js")
  },
  {
    id: "diff",
    name: "Diff",
    import: () => import("./diff-BxzP2J8R.js")
  },
  {
    id: "docker",
    name: "Dockerfile",
    aliases: ["dockerfile"],
    import: () => import("./docker-CsHqm9tx.js")
  },
  {
    id: "dotenv",
    name: "dotEnv",
    import: () => import("./dotenv-BMjVjUL1.js")
  },
  {
    id: "dream-maker",
    name: "Dream Maker",
    import: () => import("./dream-maker-PAx17jaB.js")
  },
  {
    id: "edge",
    name: "Edge",
    import: () => import("./edge-DV46E-Cp.js")
  },
  {
    id: "elixir",
    name: "Elixir",
    import: () => import("./elixir-DHwzkGR7.js")
  },
  {
    id: "elm",
    name: "Elm",
    import: () => import("./elm-BtNbw_Cd.js")
  },
  {
    id: "emacs-lisp",
    name: "Emacs Lisp",
    aliases: ["elisp"],
    import: () => import("./emacs-lisp-8iFotmUZ.js")
  },
  {
    id: "erb",
    name: "ERB",
    import: () => import("./erb-CX2WKKX7.js")
  },
  {
    id: "erlang",
    name: "Erlang",
    aliases: ["erl"],
    import: () => import("./erlang-DNcDT8Hi.js")
  },
  {
    id: "fennel",
    name: "Fennel",
    import: () => import("./fennel-N4WcXuKx.js")
  },
  {
    id: "fish",
    name: "Fish",
    import: () => import("./fish-BTDEUgqH.js")
  },
  {
    id: "fluent",
    name: "Fluent",
    aliases: ["ftl"],
    import: () => import("./fluent-BMXUxfv1.js")
  },
  {
    id: "fortran-fixed-form",
    name: "Fortran (Fixed Form)",
    aliases: [
      "f",
      "for",
      "f77"
    ],
    import: () => import("./fortran-fixed-form-B67KFkXH.js")
  },
  {
    id: "fortran-free-form",
    name: "Fortran (Free Form)",
    aliases: [
      "f90",
      "f95",
      "f03",
      "f08",
      "f18"
    ],
    import: () => import("./fortran-free-form-CwqYvCLJ.js")
  },
  {
    id: "fsharp",
    name: "F#",
    aliases: ["f#", "fs"],
    import: () => import("./fsharp-CYOiIVNx.js")
  },
  {
    id: "gdresource",
    name: "GDResource",
    aliases: ["tscn", "tres"],
    import: () => import("./gdresource-CvCIUjhi.js")
  },
  {
    id: "gdscript",
    name: "GDScript",
    aliases: ["gd"],
    import: () => import("./gdscript-DSAxJ_aS.js")
  },
  {
    id: "gdshader",
    name: "GDShader",
    import: () => import("./gdshader-Bk8fF6yr.js")
  },
  {
    id: "genie",
    name: "Genie",
    import: () => import("./genie-DzUvd7U9.js")
  },
  {
    id: "gherkin",
    name: "Gherkin",
    import: () => import("./gherkin-DHRaV0YP.js")
  },
  {
    id: "git-commit",
    name: "Git Commit Message",
    import: () => import("./git-commit-Bd32YZ0K.js")
  },
  {
    id: "git-rebase",
    name: "Git Rebase Message",
    import: () => import("./git-rebase-ZWUFO_T4.js")
  },
  {
    id: "gleam",
    name: "Gleam",
    import: () => import("./gleam-Bv284lvN.js")
  },
  {
    id: "glimmer-js",
    name: "Glimmer JS",
    aliases: ["gjs"],
    import: () => import("./glimmer-js-DvMkQjRo.js")
  },
  {
    id: "glimmer-ts",
    name: "Glimmer TS",
    aliases: ["gts"],
    import: () => import("./glimmer-ts-CWF9oSCE.js")
  },
  {
    id: "glsl",
    name: "GLSL",
    import: () => import("./glsl-CkUcVZNK.js")
  },
  {
    id: "gn",
    name: "GN",
    import: () => import("./gn-XtJxiLa4.js")
  },
  {
    id: "gnuplot",
    name: "Gnuplot",
    import: () => import("./gnuplot-yPG9-sE_.js")
  },
  {
    id: "go",
    name: "Go",
    import: () => import("./go-EbF93JXO.js")
  },
  {
    id: "graphql",
    name: "GraphQL",
    aliases: ["gql"],
    import: () => import("./graphql-CBu2ehBn.js")
  },
  {
    id: "groovy",
    name: "Groovy",
    import: () => import("./groovy-CJQTphOW.js")
  },
  {
    id: "hack",
    name: "Hack",
    import: () => import("./hack-D8lQNxiZ.js")
  },
  {
    id: "haml",
    name: "Ruby Haml",
    import: () => import("./haml-B1t5q1Sv.js")
  },
  {
    id: "handlebars",
    name: "Handlebars",
    aliases: ["hbs"],
    import: () => import("./handlebars-B2fb61mr.js")
  },
  {
    id: "haskell",
    name: "Haskell",
    aliases: ["hs"],
    import: () => import("./haskell-WeIwNIP6.js")
  },
  {
    id: "haxe",
    name: "Haxe",
    import: () => import("./haxe-TztHsm5T.js")
  },
  {
    id: "hcl",
    name: "HashiCorp HCL",
    import: () => import("./hcl-D438OF-I.js")
  },
  {
    id: "hjson",
    name: "Hjson",
    import: () => import("./hjson-DYBUbqOl.js")
  },
  {
    id: "hlsl",
    name: "HLSL",
    import: () => import("./hlsl-Bk8TCZNL.js")
  },
  {
    id: "html",
    name: "HTML",
    import: () => import("./html-CPZ3oZQ7.js")
  },
  {
    id: "html-derivative",
    name: "HTML (Derivative)",
    import: () => import("./html-derivative-B2qv7eyt.js")
  },
  {
    id: "http",
    name: "HTTP",
    import: () => import("./http-DLvAkeD-.js")
  },
  {
    id: "hurl",
    name: "Hurl",
    import: () => import("./hurl-CgkFN90t.js")
  },
  {
    id: "hxml",
    name: "HXML",
    import: () => import("./hxml-GbqSQCLa.js")
  },
  {
    id: "hy",
    name: "Hy",
    import: () => import("./hy-Brt5EZ7-.js")
  },
  {
    id: "imba",
    name: "Imba",
    import: () => import("./imba-CimUv-Uh.js")
  },
  {
    id: "ini",
    name: "INI",
    aliases: ["properties"],
    import: () => import("./ini-BZIuRIvJ.js")
  },
  {
    id: "java",
    name: "Java",
    import: () => import("./java-DY6VlHhP.js")
  },
  {
    id: "javascript",
    name: "JavaScript",
    aliases: [
      "js",
      "cjs",
      "mjs"
    ],
    import: () => import("./javascript-C25yR2R2.js")
  },
  {
    id: "jinja",
    name: "Jinja",
    import: () => import("./jinja-DFCUirAg.js")
  },
  {
    id: "jison",
    name: "Jison",
    import: () => import("./jison-DdreslRX.js")
  },
  {
    id: "json",
    name: "JSON",
    import: () => import("./json-DxJze_jm.js")
  },
  {
    id: "json5",
    name: "JSON5",
    import: () => import("./json5-BT4Fjg39.js")
  },
  {
    id: "jsonc",
    name: "JSON with Comments",
    import: () => import("./jsonc-CHjZD8gR.js")
  },
  {
    id: "jsonl",
    name: "JSON Lines",
    import: () => import("./jsonl-BGuvDmy9.js")
  },
  {
    id: "jsonnet",
    name: "Jsonnet",
    import: () => import("./jsonnet-Bx2cfsXg.js")
  },
  {
    id: "jssm",
    name: "JSSM",
    aliases: ["fsl"],
    import: () => import("./jssm-BcADi6EI.js")
  },
  {
    id: "jsx",
    name: "JSX",
    import: () => import("./jsx-BtKADgXT.js")
  },
  {
    id: "julia",
    name: "Julia",
    aliases: ["jl"],
    import: () => import("./julia-CZiKXNNP.js")
  },
  {
    id: "just",
    name: "Just",
    import: () => import("./just-C-mXeYJa.js")
  },
  {
    id: "kdl",
    name: "KDL",
    import: () => import("./kdl-BNOv9TC3.js")
  },
  {
    id: "kotlin",
    name: "Kotlin",
    aliases: ["kt", "kts"],
    import: () => import("./kotlin-ByBMgTeR.js")
  },
  {
    id: "kusto",
    name: "Kusto",
    aliases: ["kql"],
    import: () => import("./kusto-BlqsQffZ.js")
  },
  {
    id: "latex",
    name: "LaTeX",
    import: () => import("./latex-B8Egmqc5.js")
  },
  {
    id: "lean",
    name: "Lean 4",
    aliases: ["lean4"],
    import: () => import("./lean-BnVxaZxY.js")
  },
  {
    id: "less",
    name: "Less",
    import: () => import("./less-B1GLI2Di.js")
  },
  {
    id: "liquid",
    name: "Liquid",
    import: () => import("./liquid-BCMAA-JF.js")
  },
  {
    id: "llvm",
    name: "LLVM IR",
    import: () => import("./llvm-Bk2XctFf.js")
  },
  {
    id: "log",
    name: "Log file",
    import: () => import("./log-Al8wyEFV.js")
  },
  {
    id: "logo",
    name: "Logo",
    import: () => import("./logo-DBa4JDzV.js")
  },
  {
    id: "lua",
    name: "Lua",
    import: () => import("./lua-Bp3IRv7I.js")
  },
  {
    id: "luau",
    name: "Luau",
    import: () => import("./luau-UQyhudEE.js")
  },
  {
    id: "make",
    name: "Makefile",
    aliases: ["makefile"],
    import: () => import("./make-CsMclxtr.js")
  },
  {
    id: "markdown",
    name: "Markdown",
    aliases: ["md"],
    import: () => import("./markdown-CrScaQ96.js")
  },
  {
    id: "marko",
    name: "Marko",
    import: () => import("./marko-BlmC3dqW.js")
  },
  {
    id: "matlab",
    name: "MATLAB",
    import: () => import("./matlab-BOAaUVP0.js")
  },
  {
    id: "mdc",
    name: "MDC",
    import: () => import("./mdc-CLNk3q28.js")
  },
  {
    id: "mdx",
    name: "MDX",
    import: () => import("./mdx-BOhZZUJ8.js")
  },
  {
    id: "mermaid",
    name: "Mermaid",
    aliases: ["mmd"],
    import: () => import("./mermaid-C3rz4dYh.js")
  },
  {
    id: "mipsasm",
    name: "MIPS Assembly",
    aliases: ["mips"],
    import: () => import("./mipsasm-CTx18fBl.js")
  },
  {
    id: "mojo",
    name: "Mojo",
    import: () => import("./mojo-EIZIZRTQ.js")
  },
  {
    id: "moonbit",
    name: "MoonBit",
    aliases: ["mbt", "mbti"],
    import: () => import("./moonbit-B1TV0fG3.js")
  },
  {
    id: "move",
    name: "Move",
    import: () => import("./move-Db4ltDq1.js")
  },
  {
    id: "narrat",
    name: "Narrat Language",
    aliases: ["nar"],
    import: () => import("./narrat-DmhDCBs-.js")
  },
  {
    id: "nextflow",
    name: "Nextflow",
    aliases: ["nf"],
    import: () => import("./nextflow-B-DyW070.js")
  },
  {
    id: "nextflow-groovy",
    name: "Nextflow Groovy",
    import: () => import("./nextflow-groovy-BXu3YCr0.js")
  },
  {
    id: "nginx",
    name: "Nginx",
    import: () => import("./nginx-D7O4WoB1.js")
  },
  {
    id: "nim",
    name: "Nim",
    import: () => import("./nim-C-Ty7rI7.js")
  },
  {
    id: "nix",
    name: "Nix",
    import: () => import("./nix-fTRXMGic.js")
  },
  {
    id: "nushell",
    name: "nushell",
    aliases: ["nu"],
    import: () => import("./nushell-BGCPRlV5.js")
  },
  {
    id: "objective-c",
    name: "Objective-C",
    aliases: ["objc"],
    import: () => import("./objective-c-Itk8tzmv.js")
  },
  {
    id: "objective-cpp",
    name: "Objective-C++",
    import: () => import("./objective-cpp-DGt5UKRO.js")
  },
  {
    id: "ocaml",
    name: "OCaml",
    import: () => import("./ocaml-eSVK32Eg.js")
  },
  {
    id: "odin",
    name: "Odin",
    import: () => import("./odin-BItnF517.js")
  },
  {
    id: "openscad",
    name: "OpenSCAD",
    aliases: ["scad"],
    import: () => import("./openscad-Sp5uZ6f2.js")
  },
  {
    id: "pascal",
    name: "Pascal",
    import: () => import("./pascal-xy8pJNns.js")
  },
  {
    id: "perl",
    name: "Perl",
    import: () => import("./perl-DEBfTVge.js")
  },
  {
    id: "php",
    name: "PHP",
    import: () => import("./php-Kn-vgdBD.js")
  },
  {
    id: "pkl",
    name: "Pkl",
    import: () => import("./pkl-C-zSNmaA.js")
  },
  {
    id: "plsql",
    name: "PL/SQL",
    import: () => import("./plsql-pVbGZfOv.js")
  },
  {
    id: "po",
    name: "Gettext PO",
    aliases: ["pot", "potx"],
    import: () => import("./po-BNfHvqmm.js")
  },
  {
    id: "polar",
    name: "Polar",
    import: () => import("./polar-CAZahv3u.js")
  },
  {
    id: "postcss",
    name: "PostCSS",
    import: () => import("./postcss-05aHdL-n.js")
  },
  {
    id: "powerquery",
    name: "PowerQuery",
    import: () => import("./powerquery-DI9HkTvs.js")
  },
  {
    id: "powershell",
    name: "PowerShell",
    aliases: ["ps", "ps1"],
    import: () => import("./powershell-Clc4ydu-.js")
  },
  {
    id: "prisma",
    name: "Prisma",
    import: () => import("./prisma-FZjmVtSl.js")
  },
  {
    id: "prolog",
    name: "Prolog",
    import: () => import("./prolog-C5-yg4TO.js")
  },
  {
    id: "proto",
    name: "Protocol Buffer 3",
    aliases: ["protobuf"],
    import: () => import("./proto-C7QgialS.js")
  },
  {
    id: "pug",
    name: "Pug",
    aliases: ["jade"],
    import: () => import("./pug-CTOOvSEH.js")
  },
  {
    id: "puppet",
    name: "Puppet",
    import: () => import("./puppet-CUJHmnxV.js")
  },
  {
    id: "purescript",
    name: "PureScript",
    import: () => import("./purescript-rUfGld-4.js")
  },
  {
    id: "python",
    name: "Python",
    aliases: ["py"],
    import: () => import("./python-BFNSHbwJ.js")
  },
  {
    id: "qml",
    name: "QML",
    import: () => import("./qml-rTNXsquU.js")
  },
  {
    id: "qmldir",
    name: "QML Directory",
    import: () => import("./qmldir-BInDYbpo.js")
  },
  {
    id: "qss",
    name: "Qt Style Sheets",
    import: () => import("./qss-AeJTysr_.js")
  },
  {
    id: "r",
    name: "R",
    import: () => import("./r-CSmzDPi7.js")
  },
  {
    id: "racket",
    name: "Racket",
    import: () => import("./racket-B83wSAja.js")
  },
  {
    id: "raku",
    name: "Raku",
    aliases: ["perl6"],
    import: () => import("./raku-nEQ4ZJJ7.js")
  },
  {
    id: "razor",
    name: "ASP.NET Razor",
    import: () => import("./razor-C0e_FYju.js")
  },
  {
    id: "reg",
    name: "Windows Registry Script",
    import: () => import("./reg-m_s_Kiip.js")
  },
  {
    id: "regexp",
    name: "RegExp",
    aliases: ["regex"],
    import: () => import("./regexp-BazyLpPg.js")
  },
  {
    id: "rel",
    name: "Rel",
    import: () => import("./rel-BcRfyd6Q.js")
  },
  {
    id: "riscv",
    name: "RISC-V",
    import: () => import("./riscv-Ce8MAQLP.js")
  },
  {
    id: "ron",
    name: "RON",
    import: () => import("./ron-DCEEQypA.js")
  },
  {
    id: "rosmsg",
    name: "ROS Interface",
    import: () => import("./rosmsg-Cz0w1km8.js")
  },
  {
    id: "rst",
    name: "reStructuredText",
    import: () => import("./rst-15e-m2f_.js")
  },
  {
    id: "ruby",
    name: "Ruby",
    aliases: ["rb"],
    import: () => import("./ruby-BU5NlL71.js")
  },
  {
    id: "rust",
    name: "Rust",
    aliases: ["rs"],
    import: () => import("./rust-CLzF9zIN.js")
  },
  {
    id: "sas",
    name: "SAS",
    import: () => import("./sas-fpEvgATw.js")
  },
  {
    id: "sass",
    name: "Sass",
    import: () => import("./sass-DxHp5rTx.js")
  },
  {
    id: "scala",
    name: "Scala",
    import: () => import("./scala-D4grkFkl.js")
  },
  {
    id: "scheme",
    name: "Scheme",
    import: () => import("./scheme-BCRWuEm4.js")
  },
  {
    id: "scss",
    name: "SCSS",
    import: () => import("./scss-BZYVIVaN.js")
  },
  {
    id: "sdbl",
    name: "1C (Query)",
    aliases: ["1c-query"],
    import: () => import("./sdbl-B7T8abf4.js")
  },
  {
    id: "shaderlab",
    name: "ShaderLab",
    aliases: ["shader"],
    import: () => import("./shaderlab-Cr62-Wb4.js")
  },
  {
    id: "shellscript",
    name: "Shell",
    aliases: [
      "bash",
      "sh",
      "shell",
      "zsh"
    ],
    import: () => import("./shellscript-InADTalH.js")
  },
  {
    id: "shellsession",
    name: "Shell Session",
    aliases: ["console"],
    import: () => import("./shellsession-DiDJNQdy.js")
  },
  {
    id: "smalltalk",
    name: "Smalltalk",
    import: () => import("./smalltalk-BlI1_OkM.js")
  },
  {
    id: "solidity",
    name: "Solidity",
    import: () => import("./solidity-DUWnFhS6.js")
  },
  {
    id: "soy",
    name: "Closure Templates",
    aliases: ["closure-templates"],
    import: () => import("./soy-ugRnTlhU.js")
  },
  {
    id: "sparql",
    name: "SPARQL",
    import: () => import("./sparql-B0KWFEri.js")
  },
  {
    id: "splunk",
    name: "Splunk Query Language",
    aliases: ["spl"],
    import: () => import("./splunk-CRXR8A9s.js")
  },
  {
    id: "sql",
    name: "SQL",
    import: () => import("./sql-Cn_v3PB0.js")
  },
  {
    id: "ssh-config",
    name: "SSH Config",
    import: () => import("./ssh-config-DP-hNVbF.js")
  },
  {
    id: "stata",
    name: "Stata",
    import: () => import("./stata-B8c5fTjX.js")
  },
  {
    id: "stylus",
    name: "Stylus",
    aliases: ["styl"],
    import: () => import("./stylus-CyKEU1Ej.js")
  },
  {
    id: "surrealql",
    name: "SurrealQL",
    aliases: ["surql"],
    import: () => import("./surrealql-CQi7GQN7.js")
  },
  {
    id: "svelte",
    name: "Svelte",
    import: () => import("./svelte-b9PAcyUe.js")
  },
  {
    id: "swift",
    name: "Swift",
    import: () => import("./swift-DJpUqPLg.js")
  },
  {
    id: "system-verilog",
    name: "SystemVerilog",
    import: () => import("./system-verilog-BCm7smPJ.js")
  },
  {
    id: "systemd",
    name: "Systemd Units",
    import: () => import("./systemd-C-4qm6XH.js")
  },
  {
    id: "talonscript",
    name: "TalonScript",
    aliases: ["talon"],
    import: () => import("./talonscript-CFF3LF_O.js")
  },
  {
    id: "tasl",
    name: "Tasl",
    import: () => import("./tasl-Cg_WBUAe.js")
  },
  {
    id: "tcl",
    name: "Tcl",
    import: () => import("./tcl-DN7buRTF.js")
  },
  {
    id: "templ",
    name: "Templ",
    import: () => import("./templ-n4Cl1Q5M.js")
  },
  {
    id: "terraform",
    name: "Terraform",
    aliases: ["tf", "tfvars"],
    import: () => import("./terraform-DGvcn9zM.js")
  },
  {
    id: "tex",
    name: "TeX",
    import: () => import("./tex-AHxmLTL0.js")
  },
  {
    id: "toml",
    name: "TOML",
    import: () => import("./toml-DY62mUL_.js")
  },
  {
    id: "ts-tags",
    name: "TypeScript with Tags",
    aliases: ["lit"],
    import: () => import("./ts-tags-CP1b-4Z2.js")
  },
  {
    id: "tsv",
    name: "TSV",
    import: () => import("./tsv-BtvSkaG0.js")
  },
  {
    id: "tsx",
    name: "TSX",
    import: () => import("./tsx-B8rCNbgL.js")
  },
  {
    id: "turtle",
    name: "Turtle",
    import: () => import("./turtle-_H59FV7D.js")
  },
  {
    id: "twig",
    name: "Twig",
    import: () => import("./twig-DFQvaHeP.js")
  },
  {
    id: "typescript",
    name: "TypeScript",
    aliases: [
      "ts",
      "cts",
      "mts"
    ],
    import: () => import("./typescript-RycA9KXf.js")
  },
  {
    id: "typespec",
    name: "TypeSpec",
    aliases: ["tsp"],
    import: () => import("./typespec-CGUtYYRV.js")
  },
  {
    id: "typst",
    name: "Typst",
    aliases: ["typ"],
    import: () => import("./typst-D_1QKWns.js")
  },
  {
    id: "v",
    name: "V",
    import: () => import("./v-BPCNiyYe.js")
  },
  {
    id: "vala",
    name: "Vala",
    import: () => import("./vala-uxaPR7d1.js")
  },
  {
    id: "vb",
    name: "Visual Basic",
    aliases: ["cmd"],
    import: () => import("./vb-D8_c5-KN.js")
  },
  {
    id: "verilog",
    name: "Verilog",
    import: () => import("./verilog-B-bybjPF.js")
  },
  {
    id: "vhdl",
    name: "VHDL",
    import: () => import("./vhdl-CUlNa8ac.js")
  },
  {
    id: "viml",
    name: "Vim Script",
    aliases: ["vim", "vimscript"],
    import: () => import("./viml-DsfA-sWm.js")
  },
  {
    id: "vue",
    name: "Vue",
    import: () => import("./vue-CY6W2ID0.js")
  },
  {
    id: "vue-html",
    name: "Vue HTML",
    import: () => import("./vue-html-j9xNEdu3.js")
  },
  {
    id: "vue-vine",
    name: "Vue Vine",
    import: () => import("./vue-vine-DiWTpMwZ.js")
  },
  {
    id: "vyper",
    name: "Vyper",
    aliases: ["vy"],
    import: () => import("./vyper-CPQuu50u.js")
  },
  {
    id: "wasm",
    name: "WebAssembly",
    import: () => import("./wasm-BBXxrAl7.js")
  },
  {
    id: "wenyan",
    name: "Wenyan",
    aliases: ["文言"],
    import: () => import("./wenyan-pbVjoM9_.js")
  },
  {
    id: "wgsl",
    name: "WGSL",
    import: () => import("./wgsl-DY4iK1q1.js")
  },
  {
    id: "wikitext",
    name: "Wikitext",
    aliases: ["mediawiki", "wiki"],
    import: () => import("./wikitext-Z-MoUasO.js")
  },
  {
    id: "wit",
    name: "WebAssembly Interface Types",
    import: () => import("./wit-CQMQOlTg.js")
  },
  {
    id: "wolfram",
    name: "Wolfram",
    aliases: ["wl"],
    import: () => import("./wolfram-Dz4KXISs.js")
  },
  {
    id: "xml",
    name: "XML",
    import: () => import("./xml-C2J0sS9M.js")
  },
  {
    id: "xsl",
    name: "XSL",
    import: () => import("./xsl-BmSZphgC.js")
  },
  {
    id: "yaml",
    name: "YAML",
    aliases: ["yml"],
    import: () => import("./yaml-DaO7k5B1.js")
  },
  {
    id: "zenscript",
    name: "ZenScript",
    import: () => import("./zenscript-CxBjpf9c.js")
  },
  {
    id: "zig",
    name: "Zig",
    import: () => import("./zig-Vm0PO9wB.js")
  }
], Le = Object.fromEntries(oe.map((e) => [e.id, e.import])), _e = Object.fromEntries(oe.flatMap((e) => e.aliases?.map((r) => [r, e.import]) || [])), se = {
  ...Le,
  ..._e
};
var Zr = /* @__PURE__ */ ir({
  bundledLanguages: () => se,
  bundledLanguagesAlias: () => _e,
  bundledLanguagesBase: () => Le,
  bundledLanguagesInfo: () => oe,
  bundledThemes: () => ve,
  bundledThemesInfo: () => ar,
  codeToHast: () => sr,
  codeToHtml: () => or,
  codeToTokens: () => lr,
  codeToTokensBase: () => ur,
  codeToTokensWithThemes: () => pr,
  createHighlighter: () => le,
  getLastGrammarState: () => dr,
  getSingletonHighlighter: () => cr
});
const le = /* @__PURE__ */ jr({
  langs: se,
  themes: ve,
  engine: () => (0, Hr.createOnigurumaEngine)(import("./wasm-DQxwEHae.js"))
}), { codeToHtml: or, codeToHast: sr, codeToTokens: lr, codeToTokensBase: ur, codeToTokensWithThemes: pr, getSingletonHighlighter: cr, getLastGrammarState: dr } = /* @__PURE__ */ Vr(le, { guessEmbeddedLanguages: Wr }), Re = 4294967295;
var Kr = class {
  regexps;
  constructor(e, r = {}) {
    this.patterns = e, this.options = r;
    const { forgiving: t = !1, cache: n, regexConstructor: a } = r;
    if (!a) throw new Error("Option `regexConstructor` is not provided");
    this.regexps = e.map((i) => {
      if (typeof i != "string") return i;
      const o = n?.get(i);
      if (o) {
        if (o instanceof RegExp) return o;
        if (t) return null;
        throw o;
      }
      try {
        const s = a(i);
        return n?.set(i, s), s;
      } catch (s) {
        if (n?.set(i, s), t) return null;
        throw s;
      }
    });
  }
  findNextMatchSync(e, r, t) {
    const n = typeof e == "string" ? e : e.content, a = [];
    function i(o, s, l = 0) {
      return {
        index: o,
        captureIndices: s.indices.map((u) => u == null ? {
          start: Re,
          end: Re,
          length: 0
        } : {
          start: u[0] + l,
          end: u[1] + l,
          length: u[1] - u[0]
        })
      };
    }
    for (let o = 0; o < this.regexps.length; o++) {
      const s = this.regexps[o];
      if (s)
        try {
          s.lastIndex = r;
          const l = s.exec(n);
          if (!l) continue;
          if (l.index === r) return i(o, l, 0);
          a.push([
            o,
            l,
            0
          ]);
        } catch (l) {
          if (this.options.forgiving) continue;
          throw l;
        }
    }
    if (a.length) {
      const o = Math.min(...a.map((s) => s[1].index));
      for (const [s, l, u] of a) if (l.index === o) return i(s, l, u);
    }
    return null;
  }
};
function X(e) {
  if ([...e].length !== 1) throw new Error(`Expected "${e}" to be a single code point`);
  return e.codePointAt(0);
}
function Yr(e, r, t) {
  return e.has(r) || e.set(r, t), e.get(r);
}
const Me = /* @__PURE__ */ new Set(["alnum", "alpha", "ascii", "blank", "cntrl", "digit", "graph", "lower", "print", "punct", "space", "upper", "word", "xdigit"]), x = String.raw;
function Q(e, r) {
  if (e == null) throw new Error(r ?? "Value expected");
  return e;
}
const mr = x`\[\^?`, fr = `c.? | C(?:-.?)?|${x`[pP]\{(?:\^?[-\x20_]*[A-Za-z][-\x20\w]*\})?`}|${x`x[89A-Fa-f]\p{AHex}(?:\\x[89A-Fa-f]\p{AHex})*`}|${x`u(?:\p{AHex}{4})? | x\{[^\}]*\}? | x\p{AHex}{0,2}`}|${x`o\{[^\}]*\}?`}|${x`\d{1,3}`}`, Ne = /[?*+][?+]?|\{(?:\d+(?:,\d*)?|,\d+)\}\??/, re = new RegExp(x`
  \\ (?:
    ${fr}
    | [gk]<[^>]*>?
    | [gk]'[^']*'?
    | .
  )
  | \( (?:
    \? (?:
      [:=!>({]
      | <[=!]
      | <[^>]*>
      | '[^']*'
      | ~\|?
      | #(?:[^)\\]|\\.?)*
      | [^:)]*[:)]
    )?
    | \*[^\)]*\)?
  )?
  | (?:${Ne.source})+
  | ${mr}
  | .
`.replace(/\s+/g, ""), "gsu"), de = new RegExp(x`
  \\ (?:
    ${fr}
    | .
  )
  | \[:(?:\^?\p{Alpha}+|\^):\]
  | ${mr}
  | &&
  | .
`.replace(/\s+/g, ""), "gsu");
function et(e, r = {}) {
  const t = { flags: "", ...r, rules: { captureGroup: !1, singleline: !1, ...r.rules } };
  if (typeof e != "string") throw new Error("String expected as pattern");
  const n = yt(t.flags), a = [n.extended], i = { captureGroup: t.rules.captureGroup, getCurrentModX() {
    return a.at(-1);
  }, numOpenGroups: 0, popModX() {
    a.pop();
  }, pushModX(p) {
    a.push(p);
  }, replaceCurrentModX(p) {
    a[a.length - 1] = p;
  }, singleline: t.rules.singleline };
  let o = [], s;
  for (re.lastIndex = 0; s = re.exec(e); ) {
    const p = rt(i, e, s[0], re.lastIndex);
    p.tokens ? o.push(...p.tokens) : p.token && o.push(p.token), p.lastIndex !== void 0 && (re.lastIndex = p.lastIndex);
  }
  const l = [];
  let u = 0;
  o.filter((p) => p.type === "GroupOpen").forEach((p) => {
    p.kind === "capturing" ? p.number = ++u : p.raw === "(" && l.push(p);
  }), u || l.forEach((p, d) => {
    p.kind = "capturing", p.number = d + 1;
  });
  const c = u || l.length;
  return { tokens: o.map((p) => p.type === "EscapedNumber" ? xt(p, c) : p).flat(), flags: n };
}
function rt(e, r, t, n) {
  const [a, i] = t;
  if (t === "[" || t === "[^") {
    const o = tt(r, t, n);
    return { tokens: o.tokens, lastIndex: o.lastIndex };
  }
  if (a === "\\") {
    if ("AbBGyYzZ".includes(i)) return { token: Oe(t, t) };
    if (/^\\g[<']/.test(t)) {
      if (!/^\\g(?:<[^>]+>|'[^']+')$/.test(t)) throw new Error(`Invalid group name "${t}"`);
      return { token: dt(t) };
    }
    if (/^\\k[<']/.test(t)) {
      if (!/^\\k(?:<[^>]+>|'[^']+')$/.test(t)) throw new Error(`Invalid group name "${t}"`);
      return { token: hr(t) };
    }
    if (i === "K") return { token: wr("keep", t) };
    if (i === "N" || i === "R") return { token: O("newline", t, { negate: i === "N" }) };
    if (i === "O") return { token: O("any", t) };
    if (i === "X") return { token: O("text_segment", t) };
    const o = gr(t, { inCharClass: !1 });
    return Array.isArray(o) ? { tokens: o } : { token: o };
  }
  if (a === "(") {
    if (i === "*") return { token: ht(t) };
    if (t === "(?{") throw new Error(`Unsupported callout "${t}"`);
    if (t.startsWith("(?#")) {
      if (r[n] !== ")") throw new Error('Unclosed comment group "(?#"');
      return { lastIndex: n + 1 };
    }
    if (/^\(\?[-imx]+[:)]$/.test(t)) return { token: gt(t, e) };
    if (e.pushModX(e.getCurrentModX()), e.numOpenGroups++, t === "(" && !e.captureGroup || t === "(?:") return { token: W("group", t) };
    if (t === "(?>") return { token: W("atomic", t) };
    if (t === "(?=" || t === "(?!" || t === "(?<=" || t === "(?<!") return { token: W(t[2] === "<" ? "lookbehind" : "lookahead", t, { negate: t.endsWith("!") }) };
    if (t === "(" && e.captureGroup || t.startsWith("(?<") && t.endsWith(">") || t.startsWith("(?'") && t.endsWith("'")) return { token: W("capturing", t, { ...t !== "(" && { name: t.slice(3, -1) } }) };
    if (t.startsWith("(?~")) {
      if (t === "(?~|") throw new Error(`Unsupported absence function kind "${t}"`);
      return { token: W("absence_repeater", t) };
    }
    throw t === "(?(" ? new Error(`Unsupported conditional "${t}"`) : new Error(`Invalid or unsupported group option "${t}"`);
  }
  if (t === ")") {
    if (e.popModX(), e.numOpenGroups--, e.numOpenGroups < 0) throw new Error('Unmatched ")"');
    return { token: ut(t) };
  }
  if (e.getCurrentModX()) {
    if (t === "#") {
      const o = r.indexOf(`
`, n);
      return { lastIndex: o === -1 ? r.length : o };
    }
    if (/^\s$/.test(t)) {
      const o = /\s+/y;
      return o.lastIndex = n, { lastIndex: o.exec(r) ? o.lastIndex : n };
    }
  }
  if (t === ".") return { token: O("dot", t) };
  if (t === "^" || t === "$") {
    const o = e.singleline ? { "^": x`\A`, $: x`\Z` }[t] : t;
    return { token: Oe(o, t) };
  }
  return t === "|" ? { token: it(t) } : Ne.test(t) ? { tokens: kt(t) } : { token: N(X(t), t) };
}
function tt(e, r, t) {
  const n = [De(r[1] === "^", r)];
  let a = 1, i;
  for (de.lastIndex = t; i = de.exec(e); ) {
    const o = i[0];
    if (o[0] === "[" && o[1] !== ":") a++, n.push(De(o[1] === "^", o));
    else if (o === "]") {
      if (n.at(-1).type === "CharacterClassOpen") n.push(N(93, o));
      else if (a--, n.push(at(o)), !a) break;
    } else {
      const s = nt(o);
      Array.isArray(s) ? n.push(...s) : n.push(s);
    }
  }
  return { tokens: n, lastIndex: de.lastIndex || e.length };
}
function nt(e) {
  if (e[0] === "\\") return gr(e, { inCharClass: !0 });
  if (e[0] === "[") {
    const r = /\[:(?<negate>\^?)(?<name>[a-z]+):\]/.exec(e);
    if (!r || !Me.has(r.groups.name)) throw new Error(`Invalid POSIX class "${e}"`);
    return O("posix", e, { value: r.groups.name, negate: !!r.groups.negate });
  }
  return e === "-" ? ot(e) : e === "&&" ? st(e) : N(X(e), e);
}
function gr(e, { inCharClass: r }) {
  const t = e[1];
  if (t === "c" || t === "C") return ft(e);
  if ("dDhHsSwW".includes(t)) return wt(e);
  if (e.startsWith(x`\o{`)) throw new Error(`Incomplete, invalid, or unsupported octal code point "${e}"`);
  if (/^\\[pP]\{/.test(e)) {
    if (e.length === 3) throw new Error(`Incomplete or invalid Unicode property "${e}"`);
    return bt(e);
  }
  if (/^\\x[89A-Fa-f]\p{AHex}/u.test(e)) try {
    const n = e.split(/\\x/).slice(1).map((o) => parseInt(o, 16)), a = new TextDecoder("utf-8", { ignoreBOM: !0, fatal: !0 }).decode(new Uint8Array(n)), i = new TextEncoder();
    return [...a].map((o) => {
      const s = [...i.encode(o)].map((l) => `\\x${l.toString(16)}`).join("");
      return N(X(o), s);
    });
  } catch {
    throw new Error(`Multibyte code "${e}" incomplete or invalid in Oniguruma`);
  }
  if (t === "u" || t === "x") return N(Ct(e), e);
  if (Ue.has(t)) return N(Ue.get(t), e);
  if (/\d/.test(t)) return lt(r, e);
  if (e === "\\") throw new Error(x`Incomplete escape "\"`);
  if (t === "M") throw new Error(`Unsupported meta "${e}"`);
  if ([...e].length === 2) return N(e.codePointAt(1), e);
  throw new Error(`Unexpected escape "${e}"`);
}
function it(e) {
  return { type: "Alternator", raw: e };
}
function Oe(e, r) {
  return { type: "Assertion", kind: e, raw: r };
}
function hr(e) {
  return { type: "Backreference", raw: e };
}
function N(e, r) {
  return { type: "Character", value: e, raw: r };
}
function at(e) {
  return { type: "CharacterClassClose", raw: e };
}
function ot(e) {
  return { type: "CharacterClassHyphen", raw: e };
}
function st(e) {
  return { type: "CharacterClassIntersector", raw: e };
}
function De(e, r) {
  return { type: "CharacterClassOpen", negate: e, raw: r };
}
function O(e, r, t = {}) {
  return { type: "CharacterSet", kind: e, ...t, raw: r };
}
function wr(e, r, t = {}) {
  return e === "keep" ? { type: "Directive", kind: e, raw: r } : { type: "Directive", kind: e, flags: Q(t.flags), raw: r };
}
function lt(e, r) {
  return { type: "EscapedNumber", inCharClass: e, raw: r };
}
function ut(e) {
  return { type: "GroupClose", raw: e };
}
function W(e, r, t = {}) {
  return { type: "GroupOpen", kind: e, ...t, raw: r };
}
function pt(e, r, t, n) {
  return { type: "NamedCallout", kind: e, tag: r, arguments: t, raw: n };
}
function ct(e, r, t, n) {
  return { type: "Quantifier", kind: e, min: r, max: t, raw: n };
}
function dt(e) {
  return { type: "Subroutine", raw: e };
}
const mt = /* @__PURE__ */ new Set(["COUNT", "CMP", "ERROR", "FAIL", "MAX", "MISMATCH", "SKIP", "TOTAL_COUNT"]), Ue = /* @__PURE__ */ new Map([["a", 7], ["b", 8], ["e", 27], ["f", 12], ["n", 10], ["r", 13], ["t", 9], ["v", 11]]);
function ft(e) {
  const r = e[1] === "c" ? e[2] : e[3];
  if (!r || !/[A-Za-z]/.test(r)) throw new Error(`Unsupported control character "${e}"`);
  return N(X(r.toUpperCase()) - 64, e);
}
function gt(e, r) {
  let { on: t, off: n } = /^\(\?(?<on>[imx]*)(?:-(?<off>[-imx]*))?/.exec(e).groups;
  n ??= "";
  const a = (r.getCurrentModX() || t.includes("x")) && !n.includes("x"), i = je(t), o = je(n), s = {};
  if (i && (s.enable = i), o && (s.disable = o), e.endsWith(")")) return r.replaceCurrentModX(a), wr("flags", e, { flags: s });
  if (e.endsWith(":")) return r.pushModX(a), r.numOpenGroups++, W("group", e, { ...(i || o) && { flags: s } });
  throw new Error(`Unexpected flag modifier "${e}"`);
}
function ht(e) {
  const r = /\(\*(?<name>[A-Za-z_]\w*)?(?:\[(?<tag>(?:[A-Za-z_]\w*)?)\])?(?:\{(?<args>[^}]*)\})?\)/.exec(e);
  if (!r) throw new Error(`Incomplete or invalid named callout "${e}"`);
  const { name: t, tag: n, args: a } = r.groups;
  if (!t) throw new Error(`Invalid named callout "${e}"`);
  if (n === "") throw new Error(`Named callout tag with empty value not allowed "${e}"`);
  const i = a ? a.split(",").filter((c) => c !== "").map((c) => /^[+-]?\d+$/.test(c) ? +c : c) : [], [o, s, l] = i, u = mt.has(t) ? t.toLowerCase() : "custom";
  switch (u) {
    case "fail":
    case "mismatch":
    case "skip":
      if (i.length > 0) throw new Error(`Named callout arguments not allowed "${i}"`);
      break;
    case "error":
      if (i.length > 1) throw new Error(`Named callout allows only one argument "${i}"`);
      if (typeof o == "string") throw new Error(`Named callout argument must be a number "${o}"`);
      break;
    case "max":
      if (!i.length || i.length > 2) throw new Error(`Named callout must have one or two arguments "${i}"`);
      if (typeof o == "string" && !/^[A-Za-z_]\w*$/.test(o)) throw new Error(`Named callout argument one must be a tag or number "${o}"`);
      if (i.length === 2 && (typeof s == "number" || !/^[<>X]$/.test(s))) throw new Error(`Named callout optional argument two must be '<', '>', or 'X' "${s}"`);
      break;
    case "count":
    case "total_count":
      if (i.length > 1) throw new Error(`Named callout allows only one argument "${i}"`);
      if (i.length === 1 && (typeof o == "number" || !/^[<>X]$/.test(o))) throw new Error(`Named callout optional argument must be '<', '>', or 'X' "${o}"`);
      break;
    case "cmp":
      if (i.length !== 3) throw new Error(`Named callout must have three arguments "${i}"`);
      if (typeof o == "string" && !/^[A-Za-z_]\w*$/.test(o)) throw new Error(`Named callout argument one must be a tag or number "${o}"`);
      if (typeof s == "number" || !/^(?:[<>!=]=|[<>])$/.test(s)) throw new Error(`Named callout argument two must be '==', '!=', '>', '<', '>=', or '<=' "${s}"`);
      if (typeof l == "string" && !/^[A-Za-z_]\w*$/.test(l)) throw new Error(`Named callout argument three must be a tag or number "${l}"`);
      break;
    case "custom":
      throw new Error(`Undefined callout name "${t}"`);
    default:
      throw new Error(`Unexpected named callout kind "${u}"`);
  }
  return pt(u, n ?? null, a?.split(",") ?? null, e);
}
function Be(e) {
  let r = null, t, n;
  if (e[0] === "{") {
    const { minStr: a, maxStr: i } = /^\{(?<minStr>\d*)(?:,(?<maxStr>\d*))?/.exec(e).groups, o = 1e5;
    if (+a > o || i && +i > o) throw new Error("Quantifier value unsupported in Oniguruma");
    if (t = +a, n = i === void 0 ? +a : i === "" ? 1 / 0 : +i, t > n && (r = "possessive", [t, n] = [n, t]), e.endsWith("?")) {
      if (r === "possessive") throw new Error('Unsupported possessive interval quantifier chain with "?"');
      r = "lazy";
    } else r || (r = "greedy");
  } else t = e[0] === "+" ? 1 : 0, n = e[0] === "?" ? 1 : 1 / 0, r = e[1] === "+" ? "possessive" : e[1] === "?" ? "lazy" : "greedy";
  return ct(r, t, n, e);
}
function wt(e) {
  const r = e[1].toLowerCase();
  return O({ d: "digit", h: "hex", s: "space", w: "word" }[r], e, { negate: e[1] !== r });
}
function bt(e) {
  const { p: r, neg: t, value: n } = /^\\(?<p>[pP])\{(?<neg>\^?)(?<value>[^}]+)/.exec(e).groups;
  return O("property", e, { value: n, negate: r === "P" && !t || r === "p" && !!t });
}
function je(e) {
  const r = {};
  return e.includes("i") && (r.ignoreCase = !0), e.includes("m") && (r.dotAll = !0), e.includes("x") && (r.extended = !0), Object.keys(r).length ? r : null;
}
function yt(e) {
  const r = { ignoreCase: !1, dotAll: !1, extended: !1, digitIsAscii: !1, posixIsAscii: !1, spaceIsAscii: !1, wordIsAscii: !1, textSegmentMode: null };
  for (let t = 0; t < e.length; t++) {
    const n = e[t];
    if (!"imxDPSWy".includes(n)) throw new Error(`Invalid flag "${n}"`);
    if (n === "y") {
      if (!/^y{[gw]}/.test(e.slice(t))) throw new Error('Invalid or unspecified flag "y" mode');
      r.textSegmentMode = e[t + 2] === "g" ? "grapheme" : "word", t += 3;
      continue;
    }
    r[{ i: "ignoreCase", m: "dotAll", x: "extended", D: "digitIsAscii", P: "posixIsAscii", S: "spaceIsAscii", W: "wordIsAscii" }[n]] = !0;
  }
  return r;
}
function Ct(e) {
  if (/^(?:\\u(?!\p{AHex}{4})|\\x(?!\p{AHex}{1,2}|\{\p{AHex}{1,8}\}))/u.test(e)) throw new Error(`Incomplete or invalid escape "${e}"`);
  const r = e[2] === "{" ? /^\\x\{\s*(?<hex>\p{AHex}+)/u.exec(e).groups.hex : e.slice(2);
  return parseInt(r, 16);
}
function xt(e, r) {
  const { raw: t, inCharClass: n } = e, a = t.slice(1);
  if (!n && (a !== "0" && a.length === 1 || a[0] !== "0" && +a <= r)) return [hr(t)];
  const i = [], o = a.match(/^[0-7]+|\d/g);
  for (let s = 0; s < o.length; s++) {
    const l = o[s];
    let u;
    if (s === 0 && l !== "8" && l !== "9") {
      if (u = parseInt(l, 8), u > 127) throw new Error(x`Octal encoded byte above 177 unsupported "${t}"`);
    } else u = X(l);
    i.push(N(u, (s === 0 ? "\\" : "") + l));
  }
  return i;
}
function kt(e) {
  const r = [], t = new RegExp(Ne, "gy");
  let n;
  for (; n = t.exec(e); ) {
    const a = n[0];
    if (a[0] === "{") {
      const i = /^\{(?<min>\d+),(?<max>\d+)\}\??$/.exec(a);
      if (i) {
        const { min: o, max: s } = i.groups;
        if (+o > +s && a.endsWith("?")) {
          t.lastIndex--, r.push(Be(a.slice(0, -1)));
          continue;
        }
      }
    }
    r.push(Be(a));
  }
  return r;
}
function br(e, r) {
  if (!Array.isArray(e.body)) throw new Error("Expected node with body array");
  if (e.body.length !== 1) return !1;
  const t = e.body[0];
  return !r || Object.keys(r).every((n) => r[n] === t[n]);
}
function $t(e) {
  return Et.has(e.type);
}
const Et = /* @__PURE__ */ new Set(["AbsenceFunction", "Backreference", "CapturingGroup", "Character", "CharacterClass", "CharacterSet", "Group", "Quantifier", "Subroutine"]);
function yr(e, r = {}) {
  const t = { flags: "", normalizeUnknownPropertyNames: !1, skipBackrefValidation: !1, skipLookbehindValidation: !1, skipPropertyNameValidation: !1, unicodePropertyMap: null, ...r, rules: { captureGroup: !1, singleline: !1, ...r.rules } }, n = et(e, { flags: t.flags, rules: { captureGroup: t.rules.captureGroup, singleline: t.rules.singleline } }), a = (d, f) => {
    const m = n.tokens[i.nextIndex];
    switch (i.parent = d, i.nextIndex++, m.type) {
      case "Alternator":
        return D();
      case "Assertion":
        return St(m);
      case "Backreference":
        return At(m, i);
      case "Character":
        return ue(m.value, { useLastValid: !!f.isCheckingRangeEnd });
      case "CharacterClassHyphen":
        return vt(m, i, f);
      case "CharacterClassOpen":
        return It(m, i, f);
      case "CharacterSet":
        return Lt(m, i);
      case "Directive":
        return Gt(m.kind, { flags: m.flags });
      case "GroupOpen":
        return _t(m, i, f);
      case "NamedCallout":
        return Rt(m.kind, m.tag, m.arguments);
      case "Quantifier":
        return Mt(m, i);
      case "Subroutine":
        return Nt(m, i);
      default:
        throw new Error(`Unexpected token type "${m.type}"`);
    }
  }, i = { capturingGroups: [], hasNumberedRef: !1, namedGroupsByName: /* @__PURE__ */ new Map(), nextIndex: 0, normalizeUnknownPropertyNames: t.normalizeUnknownPropertyNames, parent: null, skipBackrefValidation: t.skipBackrefValidation, skipLookbehindValidation: t.skipLookbehindValidation, skipPropertyNameValidation: t.skipPropertyNameValidation, subroutines: [], tokens: n.tokens, unicodePropertyMap: t.unicodePropertyMap, walk: a }, o = Dt(Ft(n.flags));
  let s = o.body[0];
  for (; i.nextIndex < n.tokens.length; ) {
    const d = a(s, {});
    d.type === "Alternative" ? (o.body.push(d), s = d) : s.body.push(d);
  }
  const { capturingGroups: l, hasNumberedRef: u, namedGroupsByName: c, subroutines: p } = i;
  if (u && c.size && !t.rules.captureGroup) throw new Error("Numbered backref/subroutine not allowed when using named capture");
  for (const { ref: d } of p) if (typeof d == "number") {
    if (d > l.length) throw new Error("Subroutine uses a group number that's not defined");
    d && (l[d - 1].isSubroutined = !0);
  } else if (c.has(d)) {
    if (c.get(d).length > 1) throw new Error(x`Subroutine uses a duplicate group name "\g<${d}>"`);
    c.get(d)[0].isSubroutined = !0;
  } else throw new Error(x`Subroutine uses a group name that's not defined "\g<${d}>"`);
  return o;
}
function St({ kind: e }) {
  return xe(Q({ "^": "line_start", $: "line_end", "\\A": "string_start", "\\b": "word_boundary", "\\B": "word_boundary", "\\G": "search_start", "\\y": "text_segment_boundary", "\\Y": "text_segment_boundary", "\\z": "string_end", "\\Z": "string_end_newline" }[e], `Unexpected assertion kind "${e}"`), { negate: e === x`\B` || e === x`\Y` });
}
function At({ raw: e }, r) {
  const t = /^\\k[<']/.test(e), n = t ? e.slice(3, -1) : e.slice(1), a = (i, o = !1) => {
    const s = r.capturingGroups.length;
    let l = !1;
    if (i > s) if (r.skipBackrefValidation) l = !0;
    else throw new Error(`Not enough capturing groups defined to the left "${e}"`);
    return r.hasNumberedRef = !0, ke(o ? s + 1 - i : i, { orphan: l });
  };
  if (t) {
    const i = /^(?<sign>-?)0*(?<num>[1-9]\d*)$/.exec(n);
    if (i) return a(+i.groups.num, !!i.groups.sign);
    if (/[-+]/.test(n)) throw new Error(`Invalid backref name "${e}"`);
    if (!r.namedGroupsByName.has(n)) throw new Error(`Group name not defined to the left "${e}"`);
    return ke(n);
  }
  return a(+n);
}
function vt(e, r, t) {
  const { tokens: n, walk: a } = r, i = r.parent, o = i.body.at(-1), s = n[r.nextIndex];
  if (!t.isCheckingRangeEnd && o && o.type !== "CharacterClass" && o.type !== "CharacterClassRange" && s && s.type !== "CharacterClassOpen" && s.type !== "CharacterClassClose" && s.type !== "CharacterClassIntersector") {
    const l = a(i, { ...t, isCheckingRangeEnd: !0 });
    if (o.type === "Character" && l.type === "Character") return i.body.pop(), Pt(o, l);
    throw new Error("Invalid character class range");
  }
  return ue(X("-"));
}
function It({ negate: e }, r, t) {
  const { tokens: n, walk: a } = r, i = n[r.nextIndex], o = [ie()];
  let s = We(i);
  for (; s.type !== "CharacterClassClose"; ) {
    if (s.type === "CharacterClassIntersector") o.push(ie()), r.nextIndex++;
    else {
      const u = o.at(-1);
      u.body.push(a(u, t));
    }
    s = We(n[r.nextIndex], i);
  }
  const l = ie({ negate: e });
  return o.length === 1 ? l.body = o[0].body : (l.kind = "intersection", l.body = o.map((u) => u.body.length === 1 ? u.body[0] : u)), r.nextIndex++, l;
}
function Lt({ kind: e, negate: r, value: t }, n) {
  const { normalizeUnknownPropertyNames: a, skipPropertyNameValidation: i, unicodePropertyMap: o } = n;
  if (e === "property") {
    const s = pe(t);
    if (Me.has(s) && !o?.has(s)) e = "posix", t = s;
    else return z(t, { negate: r, normalizeUnknownPropertyNames: a, skipPropertyNameValidation: i, unicodePropertyMap: o });
  }
  return e === "posix" ? Ot(t, { negate: r }) : $e(e, { negate: r });
}
function _t(e, r, t) {
  const { tokens: n, capturingGroups: a, namedGroupsByName: i, skipLookbehindValidation: o, walk: s } = r, l = Ut(e), u = l.type === "AbsenceFunction", c = Ve(l), p = c && l.negate;
  if (l.type === "CapturingGroup" && (a.push(l), l.name && Yr(i, l.name, []).push(l)), u && t.isInAbsenceFunction) throw new Error("Nested absence function not supported by Oniguruma");
  let d = ze(n[r.nextIndex]);
  for (; d.type !== "GroupClose"; ) {
    if (d.type === "Alternator") l.body.push(D()), r.nextIndex++;
    else {
      const f = l.body.at(-1), m = s(f, { ...t, isInAbsenceFunction: t.isInAbsenceFunction || u, isInLookbehind: t.isInLookbehind || c, isInNegLookbehind: t.isInNegLookbehind || p });
      if (f.body.push(m), (c || t.isInLookbehind) && !o) {
        const h = "Lookbehind includes a pattern not allowed by Oniguruma";
        if (p || t.isInNegLookbehind) {
          if (He(m) || m.type === "CapturingGroup") throw new Error(h);
        } else if (He(m) || Ve(m) && m.negate) throw new Error(h);
      }
    }
    d = ze(n[r.nextIndex]);
  }
  return r.nextIndex++, l;
}
function Mt({ kind: e, min: r, max: t }, n) {
  const a = n.parent, i = a.body.at(-1);
  if (!i || !$t(i)) throw new Error("Quantifier requires a repeatable token");
  const o = xr(e, r, t, i);
  return a.body.pop(), o;
}
function Nt({ raw: e }, r) {
  const { capturingGroups: t, subroutines: n } = r;
  let a = e.slice(3, -1);
  const i = /^(?<sign>[-+]?)0*(?<num>[1-9]\d*)$/.exec(a);
  if (i) {
    const s = +i.groups.num, l = t.length;
    if (r.hasNumberedRef = !0, a = { "": s, "+": l + s, "-": l + 1 - s }[i.groups.sign], a < 1) throw new Error("Invalid subroutine number");
  } else a === "0" && (a = 0);
  const o = kr(a);
  return n.push(o), o;
}
function Tt(e, r) {
  return { type: "AbsenceFunction", kind: e, body: Y(r?.body) };
}
function D(e) {
  return { type: "Alternative", body: $r(e?.body) };
}
function xe(e, r) {
  const t = { type: "Assertion", kind: e };
  return (e === "word_boundary" || e === "text_segment_boundary") && (t.negate = !!r?.negate), t;
}
function ke(e, r) {
  const t = !!r?.orphan;
  return { type: "Backreference", ref: e, ...t && { orphan: t } };
}
function Cr(e, r) {
  const t = { name: void 0, isSubroutined: !1, ...r };
  if (t.name !== void 0 && !Bt(t.name)) throw new Error(`Group name "${t.name}" invalid in Oniguruma`);
  return { type: "CapturingGroup", number: e, ...t.name && { name: t.name }, ...t.isSubroutined && { isSubroutined: t.isSubroutined }, body: Y(r?.body) };
}
function ue(e, r) {
  const t = { useLastValid: !1, ...r };
  if (e > 1114111) {
    const n = e.toString(16);
    if (t.useLastValid) e = 1114111;
    else throw e > 1310719 ? new Error(`Invalid code point out of range "\\x{${n}}"`) : new Error(`Invalid code point out of range in JS "\\x{${n}}"`);
  }
  return { type: "Character", value: e };
}
function ie(e) {
  const r = { kind: "union", negate: !1, ...e };
  return { type: "CharacterClass", kind: r.kind, negate: r.negate, body: $r(e?.body) };
}
function Pt(e, r) {
  if (r.value < e.value) throw new Error("Character class range out of order");
  return { type: "CharacterClassRange", min: e, max: r };
}
function $e(e, r) {
  const t = !!r?.negate, n = { type: "CharacterSet", kind: e };
  return (e === "digit" || e === "hex" || e === "newline" || e === "space" || e === "word") && (n.negate = t), (e === "text_segment" || e === "newline" && !t) && (n.variableLength = !0), n;
}
function Gt(e, r = {}) {
  if (e === "keep") return { type: "Directive", kind: e };
  if (e === "flags") return { type: "Directive", kind: e, flags: Q(r.flags) };
  throw new Error(`Unexpected directive kind "${e}"`);
}
function Ft(e) {
  return { type: "Flags", ...e };
}
function L(e) {
  const r = e?.atomic, t = e?.flags;
  if (r && t) throw new Error("Atomic group cannot have flags");
  return { type: "Group", ...r && { atomic: r }, ...t && { flags: t }, body: Y(e?.body) };
}
function R(e) {
  const r = { behind: !1, negate: !1, ...e };
  return { type: "LookaroundAssertion", kind: r.behind ? "lookbehind" : "lookahead", negate: r.negate, body: Y(e?.body) };
}
function Rt(e, r, t) {
  return { type: "NamedCallout", kind: e, tag: r, arguments: t };
}
function Ot(e, r) {
  const t = !!r?.negate;
  if (!Me.has(e)) throw new Error(`Invalid POSIX class "${e}"`);
  return { type: "CharacterSet", kind: "posix", value: e, negate: t };
}
function xr(e, r, t, n) {
  if (r > t) throw new Error("Invalid reversed quantifier range");
  return { type: "Quantifier", kind: e, min: r, max: t, body: n };
}
function Dt(e, r) {
  return { type: "Regex", body: Y(r?.body), flags: e };
}
function kr(e) {
  return { type: "Subroutine", ref: e };
}
function z(e, r) {
  const t = { negate: !1, normalizeUnknownPropertyNames: !1, skipPropertyNameValidation: !1, unicodePropertyMap: null, ...r };
  let n = t.unicodePropertyMap?.get(pe(e));
  if (!n) {
    if (t.normalizeUnknownPropertyNames) n = jt(e);
    else if (t.unicodePropertyMap && !t.skipPropertyNameValidation) throw new Error(x`Invalid Unicode property "\p{${e}}"`);
  }
  return { type: "CharacterSet", kind: "property", value: n ?? e, negate: t.negate };
}
function Ut({ flags: e, kind: r, name: t, negate: n, number: a }) {
  switch (r) {
    case "absence_repeater":
      return Tt("repeater");
    case "atomic":
      return L({ atomic: !0 });
    case "capturing":
      return Cr(a, { name: t });
    case "group":
      return L({ flags: e });
    case "lookahead":
    case "lookbehind":
      return R({ behind: r === "lookbehind", negate: n });
    default:
      throw new Error(`Unexpected group kind "${r}"`);
  }
}
function Y(e) {
  if (e === void 0) e = [D()];
  else if (!Array.isArray(e) || !e.length || !e.every((r) => r.type === "Alternative")) throw new Error("Invalid body; expected array of one or more Alternative nodes");
  return e;
}
function $r(e) {
  if (e === void 0) e = [];
  else if (!Array.isArray(e) || !e.every((r) => !!r.type)) throw new Error("Invalid body; expected array of nodes");
  return e;
}
function He(e) {
  return e.type === "LookaroundAssertion" && e.kind === "lookahead";
}
function Ve(e) {
  return e.type === "LookaroundAssertion" && e.kind === "lookbehind";
}
function Bt(e) {
  return /^[\p{Alpha}\p{Pc}][^)]*$/u.test(e);
}
function jt(e) {
  return e.trim().replace(/[- _]+/g, "_").replace(/[A-Z][a-z]+(?=[A-Z])/g, "$&_").replace(/[A-Za-z]+/g, (r) => r[0].toUpperCase() + r.slice(1).toLowerCase());
}
function pe(e) {
  return e.replace(/[- _]+/g, "").toLowerCase();
}
function We(e, r) {
  return Q(e, `${r?.type === "Character" && r.value === 93 ? "Empty" : "Unclosed"} character class`);
}
function ze(e) {
  return Q(e, "Unclosed group");
}
function Z(e, r, t = null) {
  function n(i, o) {
    for (let s = 0; s < i.length; s++) {
      const l = a(i[s], o, s, i);
      s = Math.max(-1, s + l);
    }
  }
  function a(i, o = null, s = null, l = null) {
    let u = 0, c = !1;
    const p = { node: i, parent: o, key: s, container: l, root: e, remove() {
      te(l).splice(Math.max(0, j(s) + u), 1), u--, c = !0;
    }, removeAllNextSiblings() {
      return te(l).splice(j(s) + 1);
    }, removeAllPrevSiblings() {
      const b = j(s) + u;
      return u -= b, te(l).splice(0, Math.max(0, b));
    }, replaceWith(b, $ = {}) {
      const E = !!$.traverse;
      l ? l[Math.max(0, j(s) + u)] = b : Q(o, "Can't replace root node")[s] = b, E && a(b, o, s, l), c = !0;
    }, replaceWithMultiple(b, $ = {}) {
      const E = !!$.traverse;
      if (te(l).splice(Math.max(0, j(s) + u), 1, ...b), u += b.length - 1, E) {
        let k = 0;
        for (let S = 0; S < b.length; S++) k += a(b[S], o, j(s) + S + k, l);
      }
      c = !0;
    }, skip() {
      c = !0;
    } }, { type: d } = i, f = r["*"], m = r[d], h = typeof f == "function" ? f : f?.enter, w = typeof m == "function" ? m : m?.enter;
    if (h?.(p, t), w?.(p, t), !c) switch (d) {
      case "AbsenceFunction":
      case "CapturingGroup":
      case "Group":
        n(i.body, i);
        break;
      case "Alternative":
      case "CharacterClass":
        n(i.body, i);
        break;
      case "Assertion":
      case "Backreference":
      case "Character":
      case "CharacterSet":
      case "Directive":
      case "Flags":
      case "NamedCallout":
      case "Subroutine":
        break;
      case "CharacterClassRange":
        a(i.min, i, "min"), a(i.max, i, "max");
        break;
      case "LookaroundAssertion":
        n(i.body, i);
        break;
      case "Quantifier":
        a(i.body, i, "body");
        break;
      case "Regex":
        n(i.body, i), a(i.flags, i, "flags");
        break;
      default:
        throw new Error(`Unexpected node type "${d}"`);
    }
    return m?.exit?.(p, t), f?.exit?.(p, t), u;
  }
  return a(e), e;
}
function te(e) {
  if (!Array.isArray(e)) throw new Error("Container expected");
  return e;
}
function j(e) {
  if (typeof e != "number") throw new Error("Numeric key expected");
  return e;
}
const Ht = String.raw`\(\?(?:[:=!>A-Za-z\-]|<[=!]|\(DEFINE\))`;
function Vt(e, r) {
  for (let t = 0; t < e.length; t++)
    e[t] >= r && e[t]++;
}
function Wt(e, r, t, n) {
  return e.slice(0, r) + n + e.slice(r + t.length);
}
const I = Object.freeze({
  DEFAULT: "DEFAULT",
  CHAR_CLASS: "CHAR_CLASS"
});
function Te(e, r, t, n) {
  const a = new RegExp(String.raw`${r}|(?<$skip>\[\^?|\\?.)`, "gsu"), i = [!1];
  let o = 0, s = "";
  for (const l of e.matchAll(a)) {
    const { 0: u, groups: { $skip: c } } = l;
    if (!c && (!n || n === I.DEFAULT == !o)) {
      t instanceof Function ? s += t(l, {
        context: o ? I.CHAR_CLASS : I.DEFAULT,
        negated: i[i.length - 1]
      }) : s += t;
      continue;
    }
    u[0] === "[" ? (o++, i.push(u[1] === "^")) : u === "]" && o && (o--, i.pop()), s += u;
  }
  return s;
}
function Er(e, r, t, n) {
  Te(e, r, t, n);
}
function zt(e, r, t = 0, n) {
  if (!new RegExp(r, "su").test(e))
    return null;
  const a = new RegExp(`${r}|(?<$skip>\\\\?.)`, "gsu");
  a.lastIndex = t;
  let i = 0, o;
  for (; o = a.exec(e); ) {
    const { 0: s, groups: { $skip: l } } = o;
    if (!l && (!n || n === I.DEFAULT == !i))
      return o;
    s === "[" ? i++ : s === "]" && i && i--, a.lastIndex == o.index && a.lastIndex++;
  }
  return null;
}
function ne(e, r, t) {
  return !!zt(e, r, 0, t);
}
function qt(e, r) {
  const t = /\\?./gsu;
  t.lastIndex = r;
  let n = e.length, a = 0, i = 1, o;
  for (; o = t.exec(e); ) {
    const [s] = o;
    if (s === "[")
      a++;
    else if (a)
      s === "]" && a--;
    else if (s === "(")
      i++;
    else if (s === ")" && (i--, !i)) {
      n = o.index;
      break;
    }
  }
  return e.slice(r, n);
}
const qe = new RegExp(String.raw`(?<noncapturingStart>${Ht})|(?<capturingStart>\((?:\?<[^>]+>)?)|\\?.`, "gsu");
function Xt(e, r) {
  const t = r?.hiddenCaptures ?? [];
  let n = r?.captureTransfers ?? /* @__PURE__ */ new Map();
  if (!/\(\?>/.test(e))
    return {
      pattern: e,
      captureTransfers: n,
      hiddenCaptures: t
    };
  const a = "(?>", i = "(?:(?=(", o = [0], s = [];
  let l = 0, u = 0, c = NaN, p;
  do {
    p = !1;
    let d = 0, f = 0, m = !1, h;
    for (qe.lastIndex = Number.isNaN(c) ? 0 : c + i.length; h = qe.exec(e); ) {
      const { 0: w, index: b, groups: { capturingStart: $, noncapturingStart: E } } = h;
      if (w === "[")
        d++;
      else if (d)
        w === "]" && d--;
      else if (w === a && !m)
        c = b, m = !0;
      else if (m && E)
        f++;
      else if ($)
        m ? f++ : (l++, o.push(l + u));
      else if (w === ")" && m) {
        if (!f) {
          u++;
          const k = l + u;
          if (e = `${e.slice(0, c)}${i}${e.slice(c + a.length, b)}))<$$${k}>)${e.slice(b + 1)}`, p = !0, s.push(k), Vt(t, k), n.size) {
            const S = /* @__PURE__ */ new Map();
            n.forEach((ee, U) => {
              S.set(
                U >= k ? U + 1 : U,
                ee.map((B) => B >= k ? B + 1 : B)
              );
            }), n = S;
          }
          break;
        }
        f--;
      }
    }
  } while (p);
  return t.push(...s), e = Te(
    e,
    String.raw`\\(?<backrefNum>[1-9]\d*)|<\$\$(?<wrappedBackrefNum>\d+)>`,
    ({ 0: d, groups: { backrefNum: f, wrappedBackrefNum: m } }) => {
      if (f) {
        const h = +f;
        if (h > o.length - 1)
          throw new Error(`Backref "${d}" greater than number of captures`);
        return `\\${o[h]}`;
      }
      return `\\${m}`;
    },
    I.DEFAULT
  ), {
    pattern: e,
    captureTransfers: n,
    hiddenCaptures: t
  };
}
const Sr = String.raw`(?:[?*+]|\{\d+(?:,\d*)?\})`, me = new RegExp(String.raw`
\\(?: \d+
  | c[A-Za-z]
  | [gk]<[^>]+>
  | [pPu]\{[^\}]+\}
  | u[A-Fa-f\d]{4}
  | x[A-Fa-f\d]{2}
  )
| \((?: \? (?: [:=!>]
  | <(?:[=!]|[^>]+>)
  | [A-Za-z\-]+:
  | \(DEFINE\)
  ))?
| (?<qBase>${Sr})(?<qMod>[?+]?)(?<invalidQ>[?*+\{]?)
| \\?.
`.replace(/\s+/g, ""), "gsu");
function Qt(e) {
  if (!new RegExp(`${Sr}\\+`).test(e))
    return {
      pattern: e
    };
  const r = [];
  let t = null, n = null, a = "", i = 0, o;
  for (me.lastIndex = 0; o = me.exec(e); ) {
    const { 0: s, index: l, groups: { qBase: u, qMod: c, invalidQ: p } } = o;
    if (s === "[")
      i || (n = l), i++;
    else if (s === "]")
      i ? i-- : n = null;
    else if (!i)
      if (c === "+" && a && !a.startsWith("(")) {
        if (p)
          throw new Error(`Invalid quantifier "${s}"`);
        let d = -1;
        if (/^\{\d+\}$/.test(u))
          e = Wt(e, l + u.length, c, "");
        else {
          if (a === ")" || a === "]") {
            const f = a === ")" ? t : n;
            if (f === null)
              throw new Error(`Invalid unmatched "${a}"`);
            e = `${e.slice(0, f)}(?>${e.slice(f, l)}${u})${e.slice(l + s.length)}`;
          } else
            e = `${e.slice(0, l - a.length)}(?>${a}${u})${e.slice(l + s.length)}`;
          d += 4;
        }
        me.lastIndex += d;
      } else s[0] === "(" ? r.push(l) : s === ")" && (t = r.length ? r.pop() : null);
    a = s;
  }
  return {
    pattern: e
  };
}
const A = String.raw, Jt = A`\\g<(?<gRNameOrNum>[^>&]+)&R=(?<gRDepth>[^>]+)>`, Ee = A`\(\?R=(?<rDepth>[^\)]+)\)|${Jt}`, ce = A`\(\?<(?![=!])(?<captureName>[^>]+)>`, Ar = A`${ce}|(?<unnamed>\()(?!\?)`, F = new RegExp(A`${ce}|${Ee}|\(\?|\\?.`, "gsu"), fe = "Cannot use multiple overlapping recursions";
function Zt(e, r) {
  const { hiddenCaptures: t, mode: n } = {
    hiddenCaptures: [],
    mode: "plugin",
    ...r
  };
  let a = r?.captureTransfers ?? /* @__PURE__ */ new Map();
  if (!new RegExp(Ee, "su").test(e))
    return {
      pattern: e,
      captureTransfers: a,
      hiddenCaptures: t
    };
  if (n === "plugin" && ne(e, A`\(\?\(DEFINE\)`, I.DEFAULT))
    throw new Error("DEFINE groups cannot be used with recursion");
  const i = [], o = ne(e, A`\\[1-9]`, I.DEFAULT), s = /* @__PURE__ */ new Map(), l = [];
  let u = !1, c = 0, p = 0, d;
  for (F.lastIndex = 0; d = F.exec(e); ) {
    const { 0: f, groups: { captureName: m, rDepth: h, gRNameOrNum: w, gRDepth: b } } = d;
    if (f === "[")
      c++;
    else if (c)
      f === "]" && c--;
    else if (h) {
      if (Xe(h), u)
        throw new Error(fe);
      if (o)
        throw new Error(
          // When used in `external` mode by transpilers other than Regex+, backrefs might have
          // gone through conversion from named to numbered, so avoid a misleading error
          `${n === "external" ? "Backrefs" : "Numbered backrefs"} cannot be used with global recursion`
        );
      const $ = e.slice(0, d.index), E = e.slice(F.lastIndex);
      if (ne(E, Ee, I.DEFAULT))
        throw new Error(fe);
      const k = +h - 1;
      e = Qe(
        $,
        E,
        k,
        !1,
        t,
        i,
        p
      ), a = Ze(
        a,
        $,
        k,
        i.length,
        0,
        p
      );
      break;
    } else if (w) {
      Xe(b);
      let $ = !1;
      for (const J of l)
        if (J.name === w || J.num === +w) {
          if ($ = !0, J.hasRecursedWithin)
            throw new Error(fe);
          break;
        }
      if (!$)
        throw new Error(A`Recursive \g cannot be used outside the referenced group "${n === "external" ? w : A`\g<${w}&R=${b}>`}"`);
      const E = s.get(w), k = qt(e, E);
      if (o && ne(k, A`${ce}|\((?!\?)`, I.DEFAULT))
        throw new Error(
          // When used in `external` mode by transpilers other than Regex+, backrefs might have
          // gone through conversion from named to numbered, so avoid a misleading error
          `${n === "external" ? "Backrefs" : "Numbered backrefs"} cannot be used with recursion of capturing groups`
        );
      const S = e.slice(E, d.index), ee = k.slice(S.length + f.length), U = i.length, B = +b - 1, Fe = Qe(
        S,
        ee,
        B,
        !0,
        t,
        i,
        p
      );
      a = Ze(
        a,
        S,
        B,
        i.length - U,
        U,
        p
      );
      const Ur = e.slice(0, E), Br = e.slice(E + k.length);
      e = `${Ur}${Fe}${Br}`, F.lastIndex += Fe.length - f.length - S.length - ee.length, l.forEach((J) => J.hasRecursedWithin = !0), u = !0;
    } else if (m)
      p++, s.set(String(p), F.lastIndex), s.set(m, F.lastIndex), l.push({
        num: p,
        name: m
      });
    else if (f[0] === "(") {
      const $ = f === "(";
      $ && (p++, s.set(String(p), F.lastIndex)), l.push($ ? { num: p } : {});
    } else f === ")" && l.pop();
  }
  return t.push(...i), {
    pattern: e,
    captureTransfers: a,
    hiddenCaptures: t
  };
}
function Xe(e) {
  const r = `Max depth must be integer between 2 and 100; used ${e}`;
  if (!/^[1-9]\d*$/.test(e))
    throw new Error(r);
  if (e = +e, e < 2 || e > 100)
    throw new Error(r);
}
function Qe(e, r, t, n, a, i, o) {
  const s = /* @__PURE__ */ new Set();
  n && Er(e + r, ce, ({ groups: { captureName: u } }) => {
    s.add(u);
  }, I.DEFAULT);
  const l = [
    t,
    n ? s : null,
    a,
    i,
    o
  ];
  return `${e}${Je(`(?:${e}`, "forward", ...l)}(?:)${Je(`${r})`, "backward", ...l)}${r}`;
}
function Je(e, r, t, n, a, i, o) {
  const l = (c) => r === "forward" ? c + 2 : t - c + 2 - 1;
  let u = "";
  for (let c = 0; c < t; c++) {
    const p = l(c);
    u += Te(
      e,
      A`${Ar}|\\k<(?<backref>[^>]+)>`,
      ({ 0: d, groups: { captureName: f, unnamed: m, backref: h } }) => {
        if (h && n && !n.has(h))
          return d;
        const w = `_$${p}`;
        if (m || f) {
          const b = o + i.length + 1;
          return i.push(b), Kt(a, b), m ? d : `(?<${f}${w}>`;
        }
        return A`\k<${h}${w}>`;
      },
      I.DEFAULT
    );
  }
  return u;
}
function Kt(e, r) {
  for (let t = 0; t < e.length; t++)
    e[t] >= r && e[t]++;
}
function Ze(e, r, t, n, a, i) {
  if (e.size && n) {
    let o = 0;
    Er(r, Ar, () => o++, I.DEFAULT);
    const s = i - o + a, l = /* @__PURE__ */ new Map();
    return e.forEach((u, c) => {
      const p = (n - o * t) / t, d = o * t, f = c > s + o ? c + n : c, m = [];
      for (const h of u)
        if (h <= s)
          m.push(h);
        else if (h > s + o + p)
          m.push(h + n);
        else if (h <= s + o)
          for (let w = 0; w <= t; w++)
            m.push(h + o * w);
        else
          for (let w = 0; w <= t; w++)
            m.push(h + d + p * w);
      l.set(f, m);
    }), l;
  }
  return e;
}
var C = String.fromCodePoint, g = String.raw, T = {
  flagGroups: (() => {
    try {
      new RegExp("(?i:)");
    } catch {
      return !1;
    }
    return !0;
  })(),
  unicodeSets: (() => {
    try {
      new RegExp("[[]]", "v");
    } catch {
      return !1;
    }
    return !0;
  })()
};
T.bugFlagVLiteralHyphenIsRange = T.unicodeSets ? (() => {
  try {
    new RegExp(g`[\d\-a]`, "v");
  } catch {
    return !0;
  }
  return !1;
})() : !1;
T.bugNestedClassIgnoresNegation = T.unicodeSets && new RegExp("[[^a]]", "v").test("a");
function ae(e, { enable: r, disable: t }) {
  return {
    dotAll: !t?.dotAll && !!(r?.dotAll || e.dotAll),
    ignoreCase: !t?.ignoreCase && !!(r?.ignoreCase || e.ignoreCase)
  };
}
function K(e, r, t) {
  return e.has(r) || e.set(r, t), e.get(r);
}
function Se(e, r) {
  return Ke[e] >= Ke[r];
}
function Yt(e, r) {
  if (e == null)
    throw new Error(r ?? "Value expected");
  return e;
}
var Ke = {
  ES2025: 2025,
  ES2024: 2024,
  ES2018: 2018
}, en = (
  /** @type {const} */
  {
    auto: "auto",
    ES2025: "ES2025",
    ES2024: "ES2024",
    ES2018: "ES2018"
  }
);
function vr(e = {}) {
  if ({}.toString.call(e) !== "[object Object]")
    throw new Error("Unexpected options");
  if (e.target !== void 0 && !en[e.target])
    throw new Error(`Unexpected target "${e.target}"`);
  const r = {
    // Sets the level of emulation rigor/strictness.
    accuracy: "default",
    // Disables advanced emulation that relies on returning a `RegExp` subclass, resulting in
    // certain patterns not being emulatable.
    avoidSubclass: !1,
    // Oniguruma flags; a string with `i`, `m`, `x`, `D`, `S`, `W`, `y{g}` in any order (all
    // optional). Oniguruma's `m` is equivalent to JavaScript's `s` (`dotAll`).
    flags: "",
    // Include JavaScript flag `g` (`global`) in the result.
    global: !1,
    // Include JavaScript flag `d` (`hasIndices`) in the result.
    hasIndices: !1,
    // Delay regex construction until first use if the transpiled pattern is at least this length.
    lazyCompileLength: 1 / 0,
    // JavaScript version used for generated regexes. Using `auto` detects the best value based on
    // your environment. Later targets allow faster processing, simpler generated source, and
    // support for additional features.
    target: "auto",
    // Disables minifications that simplify the pattern without changing the meaning.
    verbose: !1,
    ...e,
    // Advanced options that override standard behavior, error checking, and flags when enabled.
    rules: {
      // Useful with TextMate grammars that merge backreferences across patterns.
      allowOrphanBackrefs: !1,
      // Use ASCII `\b` and `\B`, which increases search performance of generated regexes.
      asciiWordBoundaries: !1,
      // Allow unnamed captures and numbered calls (backreferences and subroutines) when using
      // named capture. This is Oniguruma option `ONIG_OPTION_CAPTURE_GROUP`; on by default in
      // `vscode-oniguruma`.
      captureGroup: !1,
      // Change the recursion depth limit from Oniguruma's `20` to an integer `2`–`20`.
      recursionLimit: 20,
      // `^` as `\A`; `$` as`\Z`. Improves search performance of generated regexes without changing
      // the meaning if searching line by line. This is Oniguruma option `ONIG_OPTION_SINGLELINE`.
      singleline: !1,
      ...e.rules
    }
  };
  return r.target === "auto" && (r.target = T.flagGroups ? "ES2025" : T.unicodeSets ? "ES2024" : "ES2018"), r;
}
var rn = "[	-\r ]", tn = /* @__PURE__ */ new Set([
  C(304),
  // İ
  C(305)
  // ı
]), _ = g`[\p{L}\p{M}\p{N}\p{Pc}]`;
function Ir(e) {
  if (tn.has(e))
    return [e];
  const r = /* @__PURE__ */ new Set(), t = e.toLowerCase(), n = t.toUpperCase(), a = on.get(t), i = nn.get(t), o = an.get(t);
  return [...n].length === 1 && r.add(n), o && r.add(o), a && r.add(a), r.add(t), i && r.add(i), [...r];
}
var Pe = /* @__PURE__ */ new Map(
  `C Other
Cc Control cntrl
Cf Format
Cn Unassigned
Co Private_Use
Cs Surrogate
L Letter
LC Cased_Letter
Ll Lowercase_Letter
Lm Modifier_Letter
Lo Other_Letter
Lt Titlecase_Letter
Lu Uppercase_Letter
M Mark Combining_Mark
Mc Spacing_Mark
Me Enclosing_Mark
Mn Nonspacing_Mark
N Number
Nd Decimal_Number digit
Nl Letter_Number
No Other_Number
P Punctuation punct
Pc Connector_Punctuation
Pd Dash_Punctuation
Pe Close_Punctuation
Pf Final_Punctuation
Pi Initial_Punctuation
Po Other_Punctuation
Ps Open_Punctuation
S Symbol
Sc Currency_Symbol
Sk Modifier_Symbol
Sm Math_Symbol
So Other_Symbol
Z Separator
Zl Line_Separator
Zp Paragraph_Separator
Zs Space_Separator
ASCII
ASCII_Hex_Digit AHex
Alphabetic Alpha
Any
Assigned
Bidi_Control Bidi_C
Bidi_Mirrored Bidi_M
Case_Ignorable CI
Cased
Changes_When_Casefolded CWCF
Changes_When_Casemapped CWCM
Changes_When_Lowercased CWL
Changes_When_NFKC_Casefolded CWKCF
Changes_When_Titlecased CWT
Changes_When_Uppercased CWU
Dash
Default_Ignorable_Code_Point DI
Deprecated Dep
Diacritic Dia
Emoji
Emoji_Component EComp
Emoji_Modifier EMod
Emoji_Modifier_Base EBase
Emoji_Presentation EPres
Extended_Pictographic ExtPict
Extender Ext
Grapheme_Base Gr_Base
Grapheme_Extend Gr_Ext
Hex_Digit Hex
IDS_Binary_Operator IDSB
IDS_Trinary_Operator IDST
ID_Continue IDC
ID_Start IDS
Ideographic Ideo
Join_Control Join_C
Logical_Order_Exception LOE
Lowercase Lower
Math
Noncharacter_Code_Point NChar
Pattern_Syntax Pat_Syn
Pattern_White_Space Pat_WS
Quotation_Mark QMark
Radical
Regional_Indicator RI
Sentence_Terminal STerm
Soft_Dotted SD
Terminal_Punctuation Term
Unified_Ideograph UIdeo
Uppercase Upper
Variation_Selector VS
White_Space space
XID_Continue XIDC
XID_Start XIDS`.split(/\s/).map((e) => [pe(e), e])
), nn = /* @__PURE__ */ new Map([
  ["s", C(383)],
  // s, ſ
  [C(383), "s"]
  // ſ, s
]), an = /* @__PURE__ */ new Map([
  [C(223), C(7838)],
  // ß, ẞ
  [C(107), C(8490)],
  // k, K (Kelvin)
  [C(229), C(8491)],
  // å, Å (Angstrom)
  [C(969), C(8486)]
  // ω, Ω (Ohm)
]), on = new Map([
  P(453),
  P(456),
  P(459),
  P(498),
  ...ge(8072, 8079),
  ...ge(8088, 8095),
  ...ge(8104, 8111),
  P(8124),
  P(8140),
  P(8188)
]), sn = /* @__PURE__ */ new Map([
  ["alnum", g`[\p{Alpha}\p{Nd}]`],
  ["alpha", g`\p{Alpha}`],
  ["ascii", g`\p{ASCII}`],
  ["blank", g`[\p{Zs}\t]`],
  ["cntrl", g`\p{Cc}`],
  ["digit", g`\p{Nd}`],
  ["graph", g`[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]`],
  ["lower", g`\p{Lower}`],
  ["print", g`[[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]\p{Zs}]`],
  ["punct", g`[\p{P}\p{S}]`],
  // Updated value from Onig 6.9.9; changed from Unicode `\p{punct}`
  ["space", g`\p{space}`],
  ["upper", g`\p{Upper}`],
  ["word", g`[\p{Alpha}\p{M}\p{Nd}\p{Pc}]`],
  ["xdigit", g`\p{AHex}`]
]);
function ln(e, r) {
  const t = [];
  for (let n = e; n <= r; n++)
    t.push(n);
  return t;
}
function P(e) {
  const r = C(e);
  return [r.toLowerCase(), r];
}
function ge(e, r) {
  return ln(e, r).map((t) => P(t));
}
var Lr = /* @__PURE__ */ new Set([
  "Lower",
  "Lowercase",
  "Upper",
  "Uppercase",
  "Ll",
  "Lowercase_Letter",
  "Lt",
  "Titlecase_Letter",
  "Lu",
  "Uppercase_Letter"
  // The `Changes_When_*` properties (and their aliases) could be included, but they're very rare.
  // Some other properties include a handful of chars with specific cases only, but these chars are
  // generally extreme edge cases and using such properties case insensitively generally produces
  // undesired behavior anyway
]);
function un(e, r) {
  const t = {
    // A couple edge cases exist where options `accuracy` and `bestEffortTarget` are used:
    // - `CharacterSet` kind `text_segment` (`\X`): An exact representation would require heavy
    //   Unicode data; a best-effort approximation requires knowing the target.
    // - `CharacterSet` kind `posix` with values `graph` and `print`: Their complex Unicode
    //   representations would be hard to change to ASCII versions after the fact in the generator
    //   based on `target`/`accuracy`, so produce the appropriate structure here.
    accuracy: "default",
    asciiWordBoundaries: !1,
    avoidSubclass: !1,
    bestEffortTarget: "ES2025",
    ...r
  };
  _r(e);
  const n = {
    accuracy: t.accuracy,
    asciiWordBoundaries: t.asciiWordBoundaries,
    avoidSubclass: t.avoidSubclass,
    flagDirectivesByAlt: /* @__PURE__ */ new Map(),
    jsGroupNameMap: /* @__PURE__ */ new Map(),
    minTargetEs2024: Se(t.bestEffortTarget, "ES2024"),
    passedLookbehind: !1,
    strategy: null,
    // Subroutines can appear before the groups they ref, so collect reffed nodes for a second pass 
    subroutineRefMap: /* @__PURE__ */ new Map(),
    supportedGNodes: /* @__PURE__ */ new Set(),
    digitIsAscii: e.flags.digitIsAscii,
    spaceIsAscii: e.flags.spaceIsAscii,
    wordIsAscii: e.flags.wordIsAscii
  };
  Z(e, pn, n);
  const a = {
    dotAll: e.flags.dotAll,
    ignoreCase: e.flags.ignoreCase
  }, i = {
    currentFlags: a,
    prevFlags: null,
    globalFlags: a,
    groupOriginByCopy: /* @__PURE__ */ new Map(),
    groupsByName: /* @__PURE__ */ new Map(),
    multiplexCapturesToLeftByRef: /* @__PURE__ */ new Map(),
    openRefs: /* @__PURE__ */ new Map(),
    reffedNodesByReferencer: /* @__PURE__ */ new Map(),
    subroutineRefMap: n.subroutineRefMap
  };
  Z(e, cn, i);
  const o = {
    groupsByName: i.groupsByName,
    highestOrphanBackref: 0,
    numCapturesToLeft: 0,
    reffedNodesByReferencer: i.reffedNodesByReferencer
  };
  return Z(e, dn, o), e._originMap = i.groupOriginByCopy, e._strategy = n.strategy, e;
}
var pn = {
  AbsenceFunction({ node: e, parent: r, replaceWith: t }) {
    const { body: n, kind: a } = e;
    if (a === "repeater") {
      const i = L();
      i.body[0].body.push(
        // Insert own alts as `body`
        R({ negate: !0, body: n }),
        z("Any")
      );
      const o = L();
      o.body[0].body.push(
        xr("greedy", 0, 1 / 0, i)
      ), t(y(o, r), { traverse: !0 });
    } else
      throw new Error('Unsupported absence function "(?~|"');
  },
  Alternative: {
    enter({ node: e, parent: r, key: t }, { flagDirectivesByAlt: n }) {
      const a = e.body.filter((i) => i.kind === "flags");
      for (let i = t + 1; i < r.body.length; i++) {
        const o = r.body[i];
        K(n, o, []).push(...a);
      }
    },
    exit({ node: e }, { flagDirectivesByAlt: r }) {
      if (r.get(e)?.length) {
        const t = Nr(r.get(e));
        if (t) {
          const n = L({ flags: t });
          n.body[0].body = e.body, e.body = [y(n, e)];
        }
      }
    }
  },
  Assertion({ node: e, parent: r, key: t, container: n, root: a, remove: i, replaceWith: o }, s) {
    const { kind: l, negate: u } = e, { asciiWordBoundaries: c, avoidSubclass: p, supportedGNodes: d, wordIsAscii: f } = s;
    if (l === "text_segment_boundary")
      throw new Error(`Unsupported text segment boundary "\\${u ? "Y" : "y"}"`);
    if (l === "line_end")
      o(y(R({ body: [
        D({ body: [xe("string_end")] }),
        D({ body: [ue(10)] })
        // `\n`
      ] }), r));
    else if (l === "line_start")
      o(y(M(g`(?<=\A|\n(?!\z))`, { skipLookbehindValidation: !0 }), r));
    else if (l === "search_start")
      if (d.has(e))
        a.flags.sticky = !0, i();
      else {
        const m = n[t - 1];
        if (m && bn(m))
          o(y(R({ negate: !0 }), r));
        else {
          if (p)
            throw new Error(g`Uses "\G" in a way that requires a subclass`);
          o(G(xe("string_start"), r)), s.strategy = "clip_search";
        }
      }
    else if (!(l === "string_end" || l === "string_start")) if (l === "string_end_newline")
      o(y(M(g`(?=\n?\z)`), r));
    else if (l === "word_boundary") {
      if (!f && !c) {
        const m = `(?:(?<=${_})(?!${_})|(?<!${_})(?=${_}))`, h = `(?:(?<=${_})(?=${_})|(?<!${_})(?!${_}))`;
        o(y(M(u ? h : m), r));
      }
    } else
      throw new Error(`Unexpected assertion kind "${l}"`);
  },
  Backreference({ node: e }, { jsGroupNameMap: r }) {
    let { ref: t } = e;
    typeof t == "string" && !we(t) && (t = he(t, r), e.ref = t);
  },
  CapturingGroup({ node: e }, { jsGroupNameMap: r, subroutineRefMap: t }) {
    let { name: n } = e;
    n && !we(n) && (n = he(n, r), e.name = n), t.set(e.number, e), n && t.set(n, e);
  },
  CharacterClassRange({ node: e, parent: r, replaceWith: t }) {
    if (r.kind === "intersection") {
      const n = ie({ body: [e] });
      t(y(n, r), { traverse: !0 });
    }
  },
  CharacterSet({ node: e, parent: r, replaceWith: t }, { accuracy: n, minTargetEs2024: a, digitIsAscii: i, spaceIsAscii: o, wordIsAscii: s }) {
    const { kind: l, negate: u, value: c } = e;
    if (i && (l === "digit" || c === "digit")) {
      t(G($e("digit", { negate: u }), r));
      return;
    }
    if (o && (l === "space" || c === "space")) {
      t(y(be(M(rn), u), r));
      return;
    }
    if (s && (l === "word" || c === "word")) {
      t(G($e("word", { negate: u }), r));
      return;
    }
    if (l === "any")
      t(G(z("Any"), r));
    else if (l === "digit")
      t(G(z("Nd", { negate: u }), r));
    else if (l !== "dot") if (l === "text_segment") {
      if (n === "strict")
        throw new Error(g`Use of "\X" requires non-strict accuracy`);
      const p = "\\p{Emoji}(?:\\p{EMod}|\\uFE0F\\u20E3?|[\\x{E0020}-\\x{E007E}]+\\x{E007F})?", d = g`\p{RI}{2}|${p}(?:\u200D${p})*`;
      t(y(M(
        // Close approximation of an extended grapheme cluster; see <unicode.org/reports/tr29/>
        g`(?>\r\n|${a ? g`\p{RGI_Emoji}` : d}|\P{M}\p{M}*)`,
        // Allow JS property `RGI_Emoji` through
        { skipPropertyNameValidation: !0 }
      ), r));
    } else if (l === "hex")
      t(G(z("AHex", { negate: u }), r));
    else if (l === "newline")
      t(y(M(u ? `[^
]` : `(?>\r
?|[
\v\f\u2028\u2029])`), r));
    else if (l === "posix")
      if (!a && (c === "graph" || c === "print")) {
        if (n === "strict")
          throw new Error(`POSIX class "${c}" requires min target ES2024 or non-strict accuracy`);
        let p = {
          graph: "!-~",
          print: " -~"
        }[c];
        u && (p = `\0-${C(p.codePointAt(0) - 1)}${C(p.codePointAt(2) + 1)}-􏿿`), t(y(M(`[${p}]`), r));
      } else
        t(y(be(M(sn.get(c)), u), r));
    else if (l === "property")
      Pe.has(pe(c)) || (e.key = "sc");
    else if (l === "space")
      t(G(z("space", { negate: u }), r));
    else if (l === "word")
      t(y(be(M(_), u), r));
    else
      throw new Error(`Unexpected character set kind "${l}"`);
  },
  Directive({ node: e, parent: r, root: t, remove: n, replaceWith: a, removeAllPrevSiblings: i, removeAllNextSiblings: o }) {
    const { kind: s, flags: l } = e;
    if (s === "flags")
      if (!l.enable && !l.disable)
        n();
      else {
        const u = L({ flags: l });
        u.body[0].body = o(), a(y(u, r), { traverse: !0 });
      }
    else if (s === "keep") {
      const u = t.body[0], p = t.body.length === 1 && // Not emulatable if within a `CapturingGroup`
      br(u, { type: "Group" }) && u.body[0].body.length === 1 ? u.body[0] : t;
      if (r.parent !== p || p.body.length > 1)
        throw new Error(g`Uses "\K" in a way that's unsupported`);
      const d = R({ behind: !0 });
      d.body[0].body = i(), a(y(d, r));
    } else
      throw new Error(`Unexpected directive kind "${s}"`);
  },
  Flags({ node: e, parent: r }) {
    if (e.posixIsAscii)
      throw new Error('Unsupported flag "P"');
    if (e.textSegmentMode === "word")
      throw new Error('Unsupported flag "y{w}"');
    [
      "digitIsAscii",
      // Flag D
      "extended",
      // Flag x
      "posixIsAscii",
      // Flag P
      "spaceIsAscii",
      // Flag S
      "wordIsAscii",
      // Flag W
      "textSegmentMode"
      // Flag y{g} or y{w}
    ].forEach((t) => delete e[t]), Object.assign(e, {
      // JS flag g; no Onig equiv
      global: !1,
      // JS flag d; no Onig equiv
      hasIndices: !1,
      // JS flag m; no Onig equiv but its behavior is always on in Onig. Onig's only line break
      // char is line feed, unlike JS, so this flag isn't used since it would produce inaccurate
      // results (also allows `^` and `$` to be used in the generator for string start and end)
      multiline: !1,
      // JS flag y; no Onig equiv, but used for `\G` emulation
      sticky: e.sticky ?? !1
      // Note: Regex+ doesn't allow explicitly adding flags it handles implicitly, so leave out
      // properties `unicode` (JS flag u) and `unicodeSets` (JS flag v). Keep the existing values
      // for `ignoreCase` (flag i) and `dotAll` (JS flag s, but Onig flag m)
    }), r.options = {
      disable: {
        // Onig uses different rules for flag x than Regex+, so disable the implicit flag
        x: !0,
        // Onig has no flag to control "named capture only" mode but contextually applies its
        // behavior when named capturing is used, so disable Regex+'s implicit flag for it
        n: !0
      },
      force: {
        // Always add flag v because we're generating an AST that relies on it (it enables JS
        // support for Onig features nested classes, intersection, Unicode properties, etc.).
        // However, the generator might disable flag v based on its `target` option
        v: !0
      }
    };
  },
  Group({ node: e }) {
    if (!e.flags)
      return;
    const { enable: r, disable: t } = e.flags;
    r?.extended && delete r.extended, t?.extended && delete t.extended, r?.dotAll && t?.dotAll && delete r.dotAll, r?.ignoreCase && t?.ignoreCase && delete r.ignoreCase, r && !Object.keys(r).length && delete e.flags.enable, t && !Object.keys(t).length && delete e.flags.disable, !e.flags.enable && !e.flags.disable && delete e.flags;
  },
  LookaroundAssertion({ node: e }, r) {
    const { kind: t } = e;
    t === "lookbehind" && (r.passedLookbehind = !0);
  },
  NamedCallout({ node: e, parent: r, replaceWith: t }) {
    const { kind: n } = e;
    if (n === "fail")
      t(y(R({ negate: !0 }), r));
    else
      throw new Error(`Unsupported named callout "(*${n.toUpperCase()}"`);
  },
  Quantifier({ node: e }) {
    if (e.body.type === "Quantifier") {
      const r = L();
      r.body[0].body.push(e.body), e.body = y(r, e);
    }
  },
  Regex: {
    enter({ node: e }, { supportedGNodes: r }) {
      const t = [];
      let n = !1, a = !1;
      for (const i of e.body)
        if (i.body.length === 1 && i.body[0].kind === "search_start")
          i.body.pop();
        else {
          const o = Pr(i.body);
          o ? (n = !0, Array.isArray(o) ? t.push(...o) : t.push(o)) : a = !0;
        }
      n && !a && t.forEach((i) => r.add(i));
    },
    exit(e, { accuracy: r, passedLookbehind: t, strategy: n }) {
      if (r === "strict" && t && n)
        throw new Error(g`Uses "\G" in a way that requires non-strict accuracy`);
    }
  },
  Subroutine({ node: e }, { jsGroupNameMap: r }) {
    let { ref: t } = e;
    typeof t == "string" && !we(t) && (t = he(t, r), e.ref = t);
  }
}, cn = {
  Backreference({ node: e }, { multiplexCapturesToLeftByRef: r, reffedNodesByReferencer: t }) {
    const { orphan: n, ref: a } = e;
    n || t.set(e, [...r.get(a).map(({ node: i }) => i)]);
  },
  CapturingGroup: {
    enter({
      node: e,
      parent: r,
      replaceWith: t,
      skip: n
    }, {
      groupOriginByCopy: a,
      groupsByName: i,
      multiplexCapturesToLeftByRef: o,
      openRefs: s,
      reffedNodesByReferencer: l
    }) {
      const u = a.get(e);
      if (u && s.has(e.number)) {
        const p = G(Ye(e.number), r);
        l.set(p, s.get(e.number)), t(p);
        return;
      }
      s.set(e.number, e), o.set(e.number, []), e.name && K(o, e.name, []);
      const c = o.get(e.name ?? e.number);
      for (let p = 0; p < c.length; p++) {
        const d = c[p];
        if (
          // This group is from subroutine expansion, and there's a multiplex value from either the
          // origin node or a prior subroutine expansion group with the same origin
          u === d.node || u && u === d.origin || // This group is not from subroutine expansion, and it comes after a subroutine expansion
          // group that refers to this group
          e === d.origin
        ) {
          c.splice(p, 1);
          break;
        }
      }
      if (o.get(e.number).push({ node: e, origin: u }), e.name && o.get(e.name).push({ node: e, origin: u }), e.name) {
        const p = K(i, e.name, /* @__PURE__ */ new Map());
        let d = !1;
        if (u)
          d = !0;
        else
          for (const f of p.values())
            if (!f.hasDuplicateNameToRemove) {
              d = !0;
              break;
            }
        i.get(e.name).set(e, { node: e, hasDuplicateNameToRemove: d });
      }
    },
    exit({ node: e }, { openRefs: r }) {
      r.delete(e.number);
    }
  },
  Group: {
    enter({ node: e }, r) {
      r.prevFlags = r.currentFlags, e.flags && (r.currentFlags = ae(r.currentFlags, e.flags));
    },
    exit(e, r) {
      r.currentFlags = r.prevFlags;
    }
  },
  Subroutine({ node: e, parent: r, replaceWith: t }, n) {
    const { isRecursive: a, ref: i } = e;
    if (a) {
      let c = r;
      for (; (c = c.parent) && !(c.type === "CapturingGroup" && (c.name === i || c.number === i)); )
        ;
      n.reffedNodesByReferencer.set(e, c);
      return;
    }
    const o = n.subroutineRefMap.get(i), s = i === 0, l = s ? Ye(0) : (
      // The reffed group might itself contain subroutines, which are expanded during sub-traversal
      Mr(o, n.groupOriginByCopy, null)
    );
    let u = l;
    if (!s) {
      const c = Nr(gn(
        o,
        (d) => d.type === "Group" && !!d.flags
      )), p = c ? ae(n.globalFlags, c) : n.globalFlags;
      mn(p, n.currentFlags) || (u = L({
        flags: hn(p)
      }), u.body[0].body.push(l));
    }
    t(y(u, r), { traverse: !s });
  }
}, dn = {
  Backreference({ node: e, parent: r, replaceWith: t }, n) {
    if (e.orphan) {
      n.highestOrphanBackref = Math.max(n.highestOrphanBackref, e.ref);
      return;
    }
    const i = n.reffedNodesByReferencer.get(e).filter((o) => fn(o, e));
    if (!i.length)
      t(y(R({ negate: !0 }), r));
    else if (i.length > 1) {
      const o = L({
        atomic: !0,
        body: i.reverse().map((s) => D({
          body: [ke(s.number)]
        }))
      });
      t(y(o, r));
    } else
      e.ref = i[0].number;
  },
  CapturingGroup({ node: e }, r) {
    e.number = ++r.numCapturesToLeft, e.name && r.groupsByName.get(e.name).get(e).hasDuplicateNameToRemove && delete e.name;
  },
  Regex: {
    exit({ node: e }, r) {
      const t = Math.max(r.highestOrphanBackref - r.numCapturesToLeft, 0);
      for (let n = 0; n < t; n++) {
        const a = Cr();
        e.body.at(-1).body.push(a);
      }
    }
  },
  Subroutine({ node: e }, r) {
    !e.isRecursive || e.ref === 0 || (e.ref = r.reffedNodesByReferencer.get(e).number);
  }
};
function _r(e) {
  Z(e, {
    "*"({ node: r, parent: t }) {
      r.parent = t;
    }
  });
}
function mn(e, r) {
  return e.dotAll === r.dotAll && e.ignoreCase === r.ignoreCase;
}
function fn(e, r) {
  let t = r;
  do {
    if (t.type === "Regex")
      return !1;
    if (t.type === "Alternative")
      continue;
    if (t === e)
      return !1;
    const n = Tr(t.parent);
    for (const a of n) {
      if (a === t)
        break;
      if (a === e || Gr(a, e))
        return !0;
    }
  } while (t = t.parent);
  throw new Error("Unexpected path");
}
function Mr(e, r, t, n) {
  const a = Array.isArray(e) ? [] : {};
  for (const [i, o] of Object.entries(e))
    i === "parent" ? a.parent = Array.isArray(t) ? n : t : o && typeof o == "object" ? a[i] = Mr(o, r, a, t) : (i === "type" && o === "CapturingGroup" && r.set(a, r.get(e) ?? e), a[i] = o);
  return a;
}
function Ye(e) {
  const r = kr(e);
  return r.isRecursive = !0, r;
}
function gn(e, r) {
  const t = [];
  for (; e = e.parent; )
    (!r || r(e)) && t.push(e);
  return t;
}
function he(e, r) {
  if (r.has(e))
    return r.get(e);
  const t = `$${r.size}_${e.replace(/^[^$_\p{IDS}]|[^$\u200C\u200D\p{IDC}]/ug, "_")}`;
  return r.set(e, t), t;
}
function Nr(e) {
  const r = ["dotAll", "ignoreCase"], t = { enable: {}, disable: {} };
  return e.forEach(({ flags: n }) => {
    r.forEach((a) => {
      n.enable?.[a] && (delete t.disable[a], t.enable[a] = !0), n.disable?.[a] && (t.disable[a] = !0);
    });
  }), Object.keys(t.enable).length || delete t.enable, Object.keys(t.disable).length || delete t.disable, t.enable || t.disable ? t : null;
}
function hn({ dotAll: e, ignoreCase: r }) {
  const t = {};
  return (e || r) && (t.enable = {}, e && (t.enable.dotAll = !0), r && (t.enable.ignoreCase = !0)), (!e || !r) && (t.disable = {}, !e && (t.disable.dotAll = !0), !r && (t.disable.ignoreCase = !0)), t;
}
function Tr(e) {
  if (!e)
    throw new Error("Node expected");
  const { body: r } = e;
  return Array.isArray(r) ? r : r ? [r] : null;
}
function Pr(e) {
  const r = e.find((t) => t.kind === "search_start" || yn(t, { negate: !1 }) || !wn(t));
  if (!r)
    return null;
  if (r.kind === "search_start")
    return r;
  if (r.type === "LookaroundAssertion")
    return r.body[0].body[0];
  if (r.type === "CapturingGroup" || r.type === "Group") {
    const t = [];
    for (const n of r.body) {
      const a = Pr(n.body);
      if (!a)
        return null;
      Array.isArray(a) ? t.push(...a) : t.push(a);
    }
    return t;
  }
  return null;
}
function Gr(e, r) {
  const t = Tr(e) ?? [];
  for (const n of t)
    if (n === r || Gr(n, r))
      return !0;
  return !1;
}
function wn({ type: e }) {
  return e === "Assertion" || e === "Directive" || e === "LookaroundAssertion";
}
function bn(e) {
  const r = [
    "Character",
    "CharacterClass",
    "CharacterSet"
  ];
  return r.includes(e.type) || e.type === "Quantifier" && e.min && r.includes(e.body.type);
}
function yn(e, r) {
  const t = {
    negate: null,
    ...r
  };
  return e.type === "LookaroundAssertion" && (t.negate === null || e.negate === t.negate) && e.body.length === 1 && br(e.body[0], {
    type: "Assertion",
    kind: "search_start"
  });
}
function we(e) {
  return /^[$_\p{IDS}][$\u200C\u200D\p{IDC}]*$/u.test(e);
}
function M(e, r) {
  const n = yr(e, {
    ...r,
    // Providing a custom set of Unicode property names avoids converting some JS Unicode
    // properties (ex: `\p{Alpha}`) to Onig POSIX classes
    unicodePropertyMap: Pe
  }).body;
  return n.length > 1 || n[0].body.length > 1 ? L({ body: n }) : n[0].body[0];
}
function be(e, r) {
  return e.negate = r, e;
}
function G(e, r) {
  return e.parent = r, e;
}
function y(e, r) {
  return _r(e), e.parent = r, e;
}
function Cn(e, r) {
  const t = vr(r), n = Se(t.target, "ES2024"), a = Se(t.target, "ES2025"), i = t.rules.recursionLimit;
  if (!Number.isInteger(i) || i < 2 || i > 20)
    throw new Error("Invalid recursionLimit; use 2-20");
  let o = null, s = null;
  if (!a) {
    const f = [e.flags.ignoreCase];
    Z(e, xn, {
      getCurrentModI: () => f.at(-1),
      popModI() {
        f.pop();
      },
      pushModI(m) {
        f.push(m);
      },
      setHasCasedChar() {
        f.at(-1) ? o = !0 : s = !0;
      }
    });
  }
  const l = {
    dotAll: e.flags.dotAll,
    // - Turn global flag i on if a case insensitive node was used and no case sensitive nodes were
    //   used (to avoid unnecessary node expansion).
    // - Turn global flag i off if a case sensitive node was used (since case sensitivity can't be
    //   forced without the use of ES2025 flag groups)
    ignoreCase: !!((e.flags.ignoreCase || o) && !s)
  };
  let u = e;
  const c = {
    accuracy: t.accuracy,
    appliedGlobalFlags: l,
    captureMap: /* @__PURE__ */ new Map(),
    currentFlags: {
      dotAll: e.flags.dotAll,
      ignoreCase: e.flags.ignoreCase
    },
    inCharClass: !1,
    lastNode: u,
    originMap: e._originMap,
    recursionLimit: i,
    useAppliedIgnoreCase: !!(!a && o && s),
    useFlagMods: a,
    useFlagV: n,
    verbose: t.verbose
  };
  function p(f) {
    return c.lastNode = u, u = f, Yt(kn[f.type], `Unexpected node type "${f.type}"`)(f, c, p);
  }
  const d = {
    pattern: e.body.map(p).join("|"),
    // Could reset `lastNode` at this point via `lastNode = ast`, but it isn't needed by flags
    flags: p(e.flags),
    options: { ...e.options }
  };
  return n || (delete d.options.force.v, d.options.disable.v = !0, d.options.unicodeSetsPlugin = null), d._captureTransfers = /* @__PURE__ */ new Map(), d._hiddenCaptures = [], c.captureMap.forEach((f, m) => {
    f.hidden && d._hiddenCaptures.push(m), f.transferTo && K(d._captureTransfers, f.transferTo, []).push(m);
  }), d;
}
var xn = {
  "*": {
    enter({ node: e }, r) {
      if (rr(e)) {
        const t = r.getCurrentModI();
        r.pushModI(
          e.flags ? ae({ ignoreCase: t }, e.flags).ignoreCase : t
        );
      }
    },
    exit({ node: e }, r) {
      rr(e) && r.popModI();
    }
  },
  Backreference(e, r) {
    r.setHasCasedChar();
  },
  Character({ node: e }, r) {
    Ge(C(e.value)) && r.setHasCasedChar();
  },
  CharacterClassRange({ node: e, skip: r }, t) {
    r(), Fr(e, { firstOnly: !0 }).length && t.setHasCasedChar();
  },
  CharacterSet({ node: e }, r) {
    e.kind === "property" && Lr.has(e.value) && r.setHasCasedChar();
  }
}, kn = {
  /**
  @param {AlternativeNode} node
  */
  Alternative({ body: e }, r, t) {
    return e.map(t).join("");
  },
  /**
  @param {AssertionNode} node
  */
  Assertion({ kind: e, negate: r }) {
    if (e === "string_end")
      return "$";
    if (e === "string_start")
      return "^";
    if (e === "word_boundary")
      return r ? g`\B` : g`\b`;
    throw new Error(`Unexpected assertion kind "${e}"`);
  },
  /**
  @param {BackreferenceNode} node
  */
  Backreference({ ref: e }, r) {
    if (typeof e != "number")
      throw new Error("Unexpected named backref in transformed AST");
    if (!r.useFlagMods && r.accuracy === "strict" && r.currentFlags.ignoreCase && !r.captureMap.get(e).ignoreCase)
      throw new Error("Use of case-insensitive backref to case-sensitive group requires target ES2025 or non-strict accuracy");
    return "\\" + e;
  },
  /**
  @param {CapturingGroupNode} node
  */
  CapturingGroup(e, r, t) {
    const { body: n, name: a, number: i } = e, o = { ignoreCase: r.currentFlags.ignoreCase }, s = r.originMap.get(e);
    return s && (o.hidden = !0, i > s.number && (o.transferTo = s.number)), r.captureMap.set(i, o), `(${a ? `?<${a}>` : ""}${n.map(t).join("|")})`;
  },
  /**
  @param {CharacterNode} node
  */
  Character({ value: e }, r) {
    const t = C(e), n = H(e, {
      escDigit: r.lastNode.type === "Backreference",
      inCharClass: r.inCharClass,
      useFlagV: r.useFlagV
    });
    if (n !== t)
      return n;
    if (r.useAppliedIgnoreCase && r.currentFlags.ignoreCase && Ge(t)) {
      const a = Ir(t);
      return r.inCharClass ? a.join("") : a.length > 1 ? `[${a.join("")}]` : a[0];
    }
    return t;
  },
  /**
  @param {CharacterClassNode} node
  */
  CharacterClass(e, r, t) {
    const { kind: n, negate: a, parent: i } = e;
    let { body: o } = e;
    if (n === "intersection" && !r.useFlagV)
      throw new Error("Use of character class intersection requires min target ES2024");
    T.bugFlagVLiteralHyphenIsRange && r.useFlagV && o.some(tr) && (o = [ue(45), ...o.filter((u) => !tr(u))]);
    const s = () => `[${a ? "^" : ""}${o.map(t).join(n === "intersection" ? "&&" : "")}]`;
    if (!r.inCharClass) {
      if (
        // Already established `kind !== 'intersection'` if `!state.useFlagV`; don't check again
        (!r.useFlagV || T.bugNestedClassIgnoresNegation) && !a
      ) {
        const c = o.filter(
          (p) => p.type === "CharacterClass" && p.kind === "union" && p.negate
        );
        if (c.length) {
          const p = L(), d = p.body[0];
          return p.parent = i, d.parent = p, o = o.filter((f) => !c.includes(f)), e.body = o, o.length ? (e.parent = d, d.body.push(e)) : p.body.pop(), c.forEach((f) => {
            const m = D({ body: [f] });
            f.parent = m, m.parent = p, p.body.push(m);
          }), t(p);
        }
      }
      r.inCharClass = !0;
      const u = s();
      return r.inCharClass = !1, u;
    }
    const l = o[0];
    if (
      // Already established that the parent is a char class via `inCharClass`; don't check again
      n === "union" && !a && l && // Allows many nested classes to work with `target` ES2018 which doesn't support nesting
      ((!r.useFlagV || !r.verbose) && i.kind === "union" && !(T.bugFlagVLiteralHyphenIsRange && r.useFlagV) || !r.verbose && i.kind === "intersection" && // JS doesn't allow intersection with union or ranges
      o.length === 1 && l.type !== "CharacterClassRange")
    )
      return o.map(t).join("");
    if (!r.useFlagV && i.type === "CharacterClass")
      throw new Error("Uses nested character class in a way that requires min target ES2024");
    return s();
  },
  /**
  @param {CharacterClassRangeNode} node
  */
  CharacterClassRange(e, r) {
    const t = e.min.value, n = e.max.value, a = {
      escDigit: !1,
      inCharClass: !0,
      useFlagV: r.useFlagV
    }, i = H(t, a), o = H(n, a), s = /* @__PURE__ */ new Set();
    if (r.useAppliedIgnoreCase && r.currentFlags.ignoreCase) {
      const l = Fr(e);
      vn(l).forEach((c) => {
        s.add(
          Array.isArray(c) ? `${H(c[0], a)}-${H(c[1], a)}` : H(c, a)
        );
      });
    }
    return `${i}-${o}${[...s].join("")}`;
  },
  /**
  @param {CharacterSetNode} node
  */
  CharacterSet({ kind: e, negate: r, value: t, key: n }, a) {
    if (e === "dot")
      return a.currentFlags.dotAll ? a.appliedGlobalFlags.dotAll || a.useFlagMods ? "." : "[^]" : (
        // Onig's only line break char is line feed, unlike JS
        g`[^\n]`
      );
    if (e === "digit")
      return r ? g`\D` : g`\d`;
    if (e === "property") {
      if (a.useAppliedIgnoreCase && a.currentFlags.ignoreCase && Lr.has(t))
        throw new Error(`Unicode property "${t}" can't be case-insensitive when other chars have specific case`);
      return `${r ? g`\P` : g`\p`}{${n ? `${n}=` : ""}${t}}`;
    }
    if (e === "word")
      return r ? g`\W` : g`\w`;
    throw new Error(`Unexpected character set kind "${e}"`);
  },
  /**
  @param {FlagsNode} node
  */
  Flags(e, r) {
    return (
      // The transformer should never turn on the properties for flags d, g, m since Onig doesn't
      // have equivs. Flag m is never used since Onig uses different line break chars than JS
      // (node.hasIndices ? 'd' : '') +
      // (node.global ? 'g' : '') +
      // (node.multiline ? 'm' : '') +
      (r.appliedGlobalFlags.ignoreCase ? "i" : "") + (e.dotAll ? "s" : "") + (e.sticky ? "y" : "")
    );
  },
  /**
  @param {GroupNode} node
  */
  Group({ atomic: e, body: r, flags: t, parent: n }, a, i) {
    const o = a.currentFlags;
    t && (a.currentFlags = ae(o, t));
    const s = r.map(i).join("|"), l = !a.verbose && r.length === 1 && // Single alt
    n.type !== "Quantifier" && !e && (!a.useFlagMods || !t) ? s : `(?${In(e, t, a.useFlagMods)}${s})`;
    return a.currentFlags = o, l;
  },
  /**
  @param {LookaroundAssertionNode} node
  */
  LookaroundAssertion({ body: e, kind: r, negate: t }, n, a) {
    return `(?${`${r === "lookahead" ? "" : "<"}${t ? "!" : "="}`}${e.map(a).join("|")})`;
  },
  /**
  @param {QuantifierNode} node
  */
  Quantifier(e, r, t) {
    return t(e.body) + Ln(e);
  },
  /**
  @param {SubroutineNode & {isRecursive: true}} node
  */
  Subroutine({ isRecursive: e, ref: r }, t) {
    if (!e)
      throw new Error("Unexpected non-recursive subroutine in transformed AST");
    const n = t.recursionLimit;
    return r === 0 ? `(?R=${n})` : g`\g<${r}&R=${n}>`;
  }
}, $n = /* @__PURE__ */ new Set([
  "$",
  "(",
  ")",
  "*",
  "+",
  ".",
  "?",
  "[",
  "\\",
  "]",
  "^",
  "{",
  "|",
  "}"
]), En = /* @__PURE__ */ new Set([
  "-",
  "\\",
  "]",
  "^",
  // Literal `[` doesn't require escaping with flag u, but this can help work around regex source
  // linters and regex syntax processors that expect unescaped `[` to create a nested class
  "["
]), Sn = /* @__PURE__ */ new Set([
  "(",
  ")",
  "-",
  "/",
  "[",
  "\\",
  "]",
  "^",
  "{",
  "|",
  "}",
  // Double punctuators; also includes already-listed `-` and `^`
  "!",
  "#",
  "$",
  "%",
  "&",
  "*",
  "+",
  ",",
  ".",
  ":",
  ";",
  "<",
  "=",
  ">",
  "?",
  "@",
  "`",
  "~"
]), er = /* @__PURE__ */ new Map([
  [9, g`\t`],
  // horizontal tab
  [10, g`\n`],
  // line feed
  [11, g`\v`],
  // vertical tab
  [12, g`\f`],
  // form feed
  [13, g`\r`],
  // carriage return
  [8232, g`\u2028`],
  // line separator
  [8233, g`\u2029`],
  // paragraph separator
  [65279, g`\uFEFF`]
  // ZWNBSP/BOM
]), An = /^\p{Cased}$/u;
function Ge(e) {
  return An.test(e);
}
function Fr(e, r) {
  const t = !!r?.firstOnly, n = e.min.value, a = e.max.value, i = [];
  if (n < 65 && (a === 65535 || a >= 131071) || n === 65536 && a >= 131071)
    return i;
  for (let o = n; o <= a; o++) {
    const s = C(o);
    if (!Ge(s))
      continue;
    const l = Ir(s).filter((u) => {
      const c = u.codePointAt(0);
      return c < n || c > a;
    });
    if (l.length && (i.push(...l), t))
      break;
  }
  return i;
}
function H(e, { escDigit: r, inCharClass: t, useFlagV: n }) {
  if (er.has(e))
    return er.get(e);
  if (
    // Control chars, etc.; condition modeled on the Chrome developer console's display for strings
    e < 32 || e > 126 && e < 160 || // Unicode planes 4-16; unassigned, special purpose, and private use area
    e > 262143 || // Avoid corrupting a preceding backref by immediately following it with a literal digit
    r && _n(e)
  )
    return e > 255 ? `\\u{${e.toString(16).toUpperCase()}}` : `\\x${e.toString(16).toUpperCase().padStart(2, "0")}`;
  const a = t ? n ? Sn : En : $n, i = C(e);
  return (a.has(i) ? "\\" : "") + i;
}
function vn(e) {
  const r = e.map((a) => a.codePointAt(0)).sort((a, i) => a - i), t = [];
  let n = null;
  for (let a = 0; a < r.length; a++)
    r[a + 1] === r[a] + 1 ? n ??= r[a] : n === null ? t.push(r[a]) : (t.push([n, r[a]]), n = null);
  return t;
}
function In(e, r, t) {
  if (e)
    return ">";
  let n = "";
  if (r && t) {
    const { enable: a, disable: i } = r;
    n = (a?.ignoreCase ? "i" : "") + (a?.dotAll ? "s" : "") + (i ? "-" : "") + (i?.ignoreCase ? "i" : "") + (i?.dotAll ? "s" : "");
  }
  return `${n}:`;
}
function Ln({ kind: e, max: r, min: t }) {
  let n;
  return !t && r === 1 ? n = "?" : !t && r === 1 / 0 ? n = "*" : t === 1 && r === 1 / 0 ? n = "+" : t === r ? n = `{${t}}` : n = `{${t},${r === 1 / 0 ? "" : r}}`, n + {
    greedy: "",
    lazy: "?",
    possessive: "+"
  }[e];
}
function rr({ type: e }) {
  return e === "CapturingGroup" || e === "Group" || e === "LookaroundAssertion";
}
function _n(e) {
  return e > 47 && e < 58;
}
function tr({ type: e, value: r }) {
  return e === "Character" && r === 45;
}
var Mn = class Ae extends RegExp {
  /**
  @type {Map<number, {
    hidden?: true;
    transferTo?: number;
  }>}
  */
  #r = /* @__PURE__ */ new Map();
  /**
  @type {RegExp | EmulatedRegExp | null}
  */
  #e = null;
  /**
  @type {string}
  */
  #n;
  /**
  @type {Map<number, string>?}
  */
  #t = null;
  /**
  @type {string?}
  */
  #i = null;
  /**
  Can be used to serialize the instance.
  @type {EmulatedRegExpOptions}
  */
  rawOptions = {};
  // Override the getter with one that works with lazy-compiled regexes
  get source() {
    return this.#n || "(?:)";
  }
  /**
  @overload
  @param {string} pattern
  @param {string} [flags]
  @param {EmulatedRegExpOptions} [options]
  */
  /**
  @overload
  @param {EmulatedRegExp} pattern
  @param {string} [flags]
  */
  constructor(r, t, n) {
    const a = !!n?.lazyCompile;
    if (r instanceof RegExp) {
      if (n)
        throw new Error("Cannot provide options when copying a regexp");
      const i = r;
      super(i, t), this.#n = i.source, i instanceof Ae && (this.#r = i.#r, this.#t = i.#t, this.#i = i.#i, this.rawOptions = i.rawOptions);
    } else {
      const i = {
        hiddenCaptures: [],
        strategy: null,
        transfers: [],
        ...n
      };
      super(a ? "" : r, t), this.#n = r, this.#r = Tn(i.hiddenCaptures, i.transfers), this.#i = i.strategy, this.rawOptions = n ?? {};
    }
    a || (this.#e = this);
  }
  /**
  Called internally by all String/RegExp methods that use regexes.
  @override
  @param {string} str
  @returns {RegExpExecArray?}
  */
  exec(r) {
    if (!this.#e) {
      const { lazyCompile: a, ...i } = this.rawOptions;
      this.#e = new Ae(this.#n, this.flags, i);
    }
    const t = this.global || this.sticky, n = this.lastIndex;
    if (this.#i === "clip_search" && t && n) {
      this.lastIndex = 0;
      const a = this.#a(r.slice(n));
      return a && (Nn(a, n, r, this.hasIndices), this.lastIndex += n), a;
    }
    return this.#a(r);
  }
  /**
  Adds support for hidden and transfer captures.
  @param {string} str
  @returns
  */
  #a(r) {
    this.#e.lastIndex = this.lastIndex;
    const t = super.exec.call(this.#e, r);
    if (this.lastIndex = this.#e.lastIndex, !t || !this.#r.size)
      return t;
    const n = [...t];
    t.length = 1;
    let a;
    this.hasIndices && (a = [...t.indices], t.indices.length = 1);
    const i = [0];
    for (let o = 1; o < n.length; o++) {
      const { hidden: s, transferTo: l } = this.#r.get(o) ?? {};
      if (s ? i.push(null) : (i.push(t.length), t.push(n[o]), this.hasIndices && t.indices.push(a[o])), l && n[o] !== void 0) {
        const u = i[l];
        if (!u)
          throw new Error(`Invalid capture transfer to "${u}"`);
        if (t[u] = n[o], this.hasIndices && (t.indices[u] = a[o]), t.groups) {
          this.#t || (this.#t = Pn(this.source));
          const c = this.#t.get(l);
          c && (t.groups[c] = n[o], this.hasIndices && (t.indices.groups[c] = a[o]));
        }
      }
    }
    return t;
  }
};
function Nn(e, r, t, n) {
  if (e.index += r, e.input = t, n) {
    const a = e.indices;
    for (let o = 0; o < a.length; o++) {
      const s = a[o];
      s && (a[o] = [s[0] + r, s[1] + r]);
    }
    const i = a.groups;
    i && Object.keys(i).forEach((o) => {
      const s = i[o];
      s && (i[o] = [s[0] + r, s[1] + r]);
    });
  }
}
function Tn(e, r) {
  const t = /* @__PURE__ */ new Map();
  for (const n of e)
    t.set(n, {
      hidden: !0
    });
  for (const [n, a] of r)
    for (const i of a)
      K(t, i, {}).transferTo = n;
  return t;
}
function Pn(e) {
  const r = /(?<capture>\((?:\?<(?![=!])(?<name>[^>]+)>|(?!\?)))|\\?./gsu, t = /* @__PURE__ */ new Map();
  let n = 0, a = 0, i;
  for (; i = r.exec(e); ) {
    const { 0: o, groups: { capture: s, name: l } } = i;
    o === "[" ? n++ : n ? o === "]" && n-- : s && (a++, l && t.set(a, l));
  }
  return t;
}
function Gn(e, r) {
  const t = Fn(e, r);
  return t.options ? new Mn(t.pattern, t.flags, t.options) : new RegExp(t.pattern, t.flags);
}
function Fn(e, r) {
  const t = vr(r), n = yr(e, {
    flags: t.flags,
    normalizeUnknownPropertyNames: !0,
    rules: {
      captureGroup: t.rules.captureGroup,
      singleline: t.rules.singleline
    },
    skipBackrefValidation: t.rules.allowOrphanBackrefs,
    unicodePropertyMap: Pe
  }), a = un(n, {
    accuracy: t.accuracy,
    asciiWordBoundaries: t.rules.asciiWordBoundaries,
    avoidSubclass: t.avoidSubclass,
    bestEffortTarget: t.target
  }), i = Cn(a, t), o = Zt(i.pattern, {
    captureTransfers: i._captureTransfers,
    hiddenCaptures: i._hiddenCaptures,
    mode: "external"
  }), s = Qt(o.pattern), l = Xt(s.pattern, {
    captureTransfers: o.captureTransfers,
    hiddenCaptures: o.hiddenCaptures
  }), u = {
    pattern: l.pattern,
    flags: `${t.hasIndices ? "d" : ""}${t.global ? "g" : ""}${i.flags}${i.options.disable.v ? "u" : "v"}`
  };
  if (t.avoidSubclass) {
    if (t.lazyCompileLength !== 1 / 0)
      throw new Error("Lazy compilation requires subclass");
  } else {
    const c = l.hiddenCaptures.sort((m, h) => m - h), p = Array.from(l.captureTransfers), d = a._strategy, f = u.pattern.length >= t.lazyCompileLength;
    (c.length || p.length || d || f) && (u.options = {
      ...c.length && { hiddenCaptures: c },
      ...p.length && { transfers: p },
      ...d && { strategy: d },
      ...f && { lazyCompile: f }
    });
  }
  return u;
}
function Rr(e, r) {
  return Gn(e, {
    global: !0,
    hasIndices: !0,
    lazyCompileLength: 3e3,
    rules: {
      allowOrphanBackrefs: !0,
      asciiWordBoundaries: !0,
      captureGroup: !0,
      recursionLimit: 5,
      singleline: !0
    },
    ...r
  });
}
function Or(e = {}) {
  const r = Object.assign({
    target: "auto",
    cache: /* @__PURE__ */ new Map()
  }, e);
  return r.regexConstructor ||= (t) => Rr(t, { target: r.target }), {
    createScanner(t) {
      return new Kr(t, r);
    },
    createString(t) {
      return { content: t };
    }
  };
}
var Rn = /* @__PURE__ */ ir({
  bundledLanguages: () => se,
  bundledLanguagesAlias: () => _e,
  bundledLanguagesBase: () => Le,
  bundledLanguagesInfo: () => oe,
  bundledThemes: () => ve,
  bundledThemesInfo: () => ar,
  codeToHast: () => sr,
  codeToHtml: () => or,
  codeToTokens: () => lr,
  codeToTokensBase: () => ur,
  codeToTokensWithThemes: () => pr,
  createHighlighter: () => le,
  createJavaScriptRegexEngine: () => Or,
  createOnigurumaEngine: () => Xr,
  defaultJavaScriptRegexConstructor: () => Rr,
  getLastGrammarState: () => dr,
  getSingletonHighlighter: () => cr,
  loadWasm: () => qr
});
zr(Rn, Zr);
var On = Ie("block", "before:content-[counter(line)]", "before:inline-block", "before:[counter-increment:line]", "before:w-4", "before:mr-4", "before:text-[13px]", "before:text-right", "before:text-muted-foreground/50", "before:font-mono", "before:select-none"), Dn = q.memo(({ children: e, result: r, language: t, className: n, ...a }) => {
  let i = q.useMemo(() => ({ backgroundColor: r.bg, color: r.fg }), [r.bg, r.fg]);
  return v.jsx("pre", { className: Ie(n, "p-4 text-sm dark:bg-(--shiki-dark-bg)!"), "data-language": t, "data-streamdown": "code-block-body", style: i, ...a, children: v.jsx("code", { className: "[counter-increment:line_0] [counter-reset:line]", children: r.tokens.map((o, s) => v.jsx("span", { className: On, children: o.map((l, u) => v.jsx("span", { className: "dark:bg-(--shiki-dark-bg)! dark:text-(--shiki-dark)!", style: { color: l.color, backgroundColor: l.bgColor, ...l.htmlStyle }, ...l.htmlAttrs, children: l.content }, u)) }, s)) }) });
}, (e, r) => e.result === r.result && e.language === r.language && e.className === r.className), Un = ({ className: e, language: r, style: t, ...n }) => v.jsx("div", { className: Ie("my-4 w-full overflow-hidden rounded-xl border border-border", e), "data-language": r, "data-streamdown": "code-block", style: { contentVisibility: "auto", containIntrinsicSize: "auto 200px", ...t }, ...n }), Bn = ({ language: e, children: r }) => v.jsxs("div", { className: "flex items-center justify-between bg-muted/80 p-3 text-muted-foreground text-xs", "data-language": e, "data-streamdown": "code-block-header", children: [v.jsx("span", { className: "ml-1 font-mono lowercase", children: e }), v.jsx("div", { className: "flex items-center gap-2", children: r })] }), jn = Or({ forgiving: !0 }), ye = /* @__PURE__ */ new Map(), Ce = /* @__PURE__ */ new Map(), V = /* @__PURE__ */ new Map(), Hn = (e, r) => `${e}-${r[0]}-${r[1]}`, Vn = (e, r, t) => {
  let n = e.slice(0, 100), a = e.length > 100 ? e.slice(-100) : "";
  return `${r}:${t[0]}:${t[1]}:${e.length}:${n}:${a}`;
}, Dr = (e) => Object.hasOwn(se, e), Wn = (e, r) => {
  let t = Dr(e) ? e : "text", n = Hn(t, r);
  if (ye.has(n)) return ye.get(n);
  let a = le({ themes: r, langs: [t], engine: jn });
  return ye.set(n, a), a;
}, nr = (e, r, t, n) => {
  let a = Dr(r) ? r : "text", i = Vn(e, a, t);
  return Ce.has(i) ? Ce.get(i) : (n && (V.has(i) || V.set(i, /* @__PURE__ */ new Set()), V.get(i).add(n)), Wn(a, t).then((o) => {
    let s = o.codeToTokens(e, { lang: a, themes: { light: t[0], dark: t[1] } });
    Ce.set(i, s);
    let l = V.get(i);
    if (l) {
      for (let u of l) u(s);
      V.delete(i);
    }
  }).catch((o) => {
    console.error("Failed to highlight code:", o), V.delete(i);
  }), null);
}, Xn = ({ code: e, language: r, className: t, children: n, ...a }) => {
  let { shikiTheme: i } = q.useContext(Qr), o = q.useMemo(() => ({ bg: "transparent", fg: "inherit", tokens: e.split(`
`).map((u) => [{ content: u, color: "inherit", bgColor: "transparent", htmlStyle: {}, offset: 0 }]) }), [e]), [s, l] = q.useState(o);
  return q.useEffect(() => {
    let u = nr(e, r, i);
    if (u) {
      l(u);
      return;
    }
    nr(e, r, i, (c) => {
      l(c);
    });
  }, [e, r, i]), v.jsx(Jr.Provider, { value: { code: e }, children: v.jsxs(Un, { language: r, children: [v.jsx(Bn, { language: r, children: n }), v.jsx(Dn, { className: t, language: r, result: s, ...a })] }) });
};
export {
  Xn as CodeBlock
};
